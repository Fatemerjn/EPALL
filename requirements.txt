# Pinned to the validated environment (Python 3.13).
# Deep-learning stack (required to run main.py / methods / models):
torch==2.9.1
torchvision==0.24.1
# Analysis / results stack (required by tools/):
numpy==2.4.1
pandas==2.3.3
matplotlib==3.10.9
#
# Optional extras -- every use below is guarded and falls back to a
# self-contained implementation, so none of these are needed to reproduce the
# reported numbers:
#   scipy           faster Clopper-Pearson bounds in tools/audit_privacy.py
#   scikit-learn    logistic-regression linear probe in main.py (else SGD)
#   arabic-reshaper, python-bidi   Persian axis labels in tools/make_paper_figures.py
