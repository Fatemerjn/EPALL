\chapter{آزمایش‌ها و نتایج}

در فصل‌های پیشین، مسئله‌ی فراموشی انتخابی آگاه از هم‌پوشانی تعریف شد و دو روش پیشنهادی، یعنی \lr{PALL-Modified} و \lr{PALL-Adapter}، معرفی گردیدند. هدف این فصل ارزیابی تجربی این دو روش و پاسخ به پرسش‌های پژوهش است. ارزیابی باید نشان دهد که روش‌ها تنها از نظر فرمول‌بندی جذاب نیستند، بلکه در دنباله‌های واقعی آموزش و فراموشی می‌توانند سه هدف متعارض را هم‌زمان مدیریت کنند: حذف دانش وظیفه‌ی هدف، حفظ عملکرد وظایف فعال، و کاهش هزینه‌ی پارامتری.

نتایج این فصل بر خروجی‌های تجمیعی اجراهای پروژه تکیه دارد. برای جلوگیری از ابهام، هرجا که نتایج از اجرای چند بذر یا چند پیکربندی آمده‌اند، میانگین و انحراف معیار گزارش می‌شود؛ و هرجا اجرای \lr{TinyImageNet} تنها به‌صورت اجرای مقدماتی صحت‌سنجی یا تک‌بذر موجود بوده، این محدودیت صریحا بیان می‌شود. هدف، ارائه‌ی تصویری دقیق و قابل دفاع از وضعیت فعلی پروژه است، نه بزرگ‌نمایی نتایج.

\section{پروتکل کلی آزمایش}

آزمایش‌ها در تنظیم یادگیری پیوسته‌ی وظیفه‌آگاه انجام شده‌اند. مدل دنباله‌ای از درخواست‌ها را دریافت می‌کند. هر درخواست یا از نوع آموزش یک وظیفه‌ی جدید است یا از نوع فراموشی یک وظیفه‌ی پیش‌تر آموزش‌دیده. در درخواست آموزش، مدل فقط داده‌ی وظیفه‌ی جاری را می‌بیند و با توجه به روش انتخاب‌شده، یا کل شبکه، یا زیرشبکه‌ی متناظر، یا آداپترهای کوچک را به‌روزرسانی می‌کند. در درخواست فراموشی، مدل باید اثر وظیفه‌ی هدف را حذف کند و سپس روی وظایف فعال باقی‌مانده ارزیابی شود.

برای تکرارپذیری، دنباله‌های درخواست پیش از اجرا به‌صورت فایل‌های ساخت‌یافته تثبیت شده‌اند؛ برای هر محک یک دنباله‌ی پایه با تعداد مشخصی وظیفه و چند درخواست فراموشی تعریف شده است (برای نمونه، پنج وظیفه و سه درخواست فراموشی در محک نخست، و تعداد بیشتری وظیفه در محک‌های بزرگ‌تر). این دنباله‌ها مشخص می‌کنند کدام وظایف و در چه ترتیبی آموزش یا فراموش می‌شوند. استفاده از دنباله‌ی ثابت اهمیت زیادی دارد، زیرا مقایسه‌ی روش‌های فراموشی تنها زمانی منصفانه است که همه‌ی آن‌ها با همان ترتیب درخواست‌ها روبه‌رو شوند.

معماری پایه در آزمایش‌ها \lr{ResNet-18} است \cite{he2016deep}. در روش‌های تمام‌شبکه مانند \lr{PALL-Original} و \lr{PALL-Modified}، ساختار زیرشبکه‌ای و ماسک‌های وظیفه‌ای روی این معماری اعمال می‌شوند. در \lr{PALL-Adapter}، بدنه‌ی اصلی منجمد است و آموزش و فراموشی از طریق آداپترهای گلوگاهی انجام می‌شود. تنظیم پایه‌ی آداپتر در نتایج اصلی شامل بعد گلوگاه ۱۶، نسبت فراموشی مشترک $\alpha_f=0.3$ و نسبت محافظت مشترک $\alpha_p=0.2$ است. در بخش‌های بعدی نشان داده می‌شود که این تنظیم، نقطه‌ای عملی میان کارایی پارامتری و پایداری فراهم می‌کند.

\subsection{فرآیند تولید جدول‌ها}

برای جلوگیری از خطای دستی در انتقال نتایج، خروجی هر اجرا ابتدا به‌صورت فایل‌های ساخت‌یافته‌ی معیارها ذخیره شده و سپس با ابزارهای تجمیع به جدول‌های تحلیلی تبدیل شده است. جدول‌های اصلی این فصل—از جمله جدول تفصیلی نتایج روش‌های \lr{PALL}، خلاصه‌ی مطالعه‌ی آداپتر، و خلاصه‌ی همبستگی هم‌پوشانی—از همین خروجی‌های تجمیعی استخراج شده‌اند. شکل‌های \ref{fig:worstdrop-results}، \ref{fig:tradeoff-results} و \ref{fig:overlap-results} نیز با ابزار رسم شکل‌های پروژه از داده‌های واقعی اجراها ساخته شده‌اند.

این مسیر تولید نتیجه دو مزیت دارد. نخست، هر عدد در جدول قابل ردیابی به یک اجرای مشخص است. دوم، اگر آزمایش‌ها با بذر یا دنباله‌ی جدید تکرار شوند، جدول‌ها و شکل‌ها بدون تغییر دستی در متن قابل بازتولید هستند. چنین جداسازی‌ای برای پایان‌نامه مهم است، زیرا خطای انتقال عددی در جدول‌های پژوهشی یکی از رایج‌ترین منابع ناسازگاری میان متن و کد است.

\subsection{تنظیمات و ابرپارامترها}

برای شفافیت و بازتولیدپذیری، تنظیمات اصلی آزمایش‌ها در جدول \ref{tab:hparams} گرد آمده است. این تنظیمات در همه‌ی اجراهای اصلی ثابت نگه داشته شده‌اند، مگر آن‌که در تحلیل حساسیت به‌صراحت تغییر داده شوند. هدف از تثبیت این مقادیر آن است که تفاوت مشاهده‌شده میان روش‌ها به خود سازوکار فراموشی نسبت داده شود، نه به تفاوت تنظیمات.

\begin{table}[t]
\centering
\caption{تنظیمات اصلی آزمایش‌ها و ابرپارامترهای روش‌های پیشنهادی}
\label{tab:hparams}
\begin{tabular}{p{0.46\columnwidth}p{0.34\columnwidth}}
\toprule
\textbf{تنظیم} & \textbf{مقدار} \\
\midrule
معماری پایه & شبکه‌ی باقی‌مانده‌ی هجده‌لایه \\
اندازه‌ی دسته & ۳۲ \\
بهینه‌ساز & گرادیان کاهشی تصادفی \\
نرخ یادگیری & $0.01$ \\
مومنتوم & $0.9$ \\
ضریب فروپاشی وزن & $5\times10^{-4}$ \\
افراز محک نخست & ۵ وظیفه‌ی دوکلاسه \\
افراز محک دوم & ۱۰ وظیفه‌ی پنج‌کلاسه \\
افراز محک سوم & ۲۰ وظیفه‌ی ده‌کلاسه \\
نسبت محافظت (روش تمام‌شبکه) & $0.2$ \\
ضریب جریمه‌ی محافظتی & $0.1$ \\
بعد گلوگاه آداپتر & ۱۶ \\
نسبت فراموشی مشترک ($\alpha_f$) & $0.3$ \\
نسبت محافظت مشترک ($\alpha_p$) & $0.2$ \\
\bottomrule
\end{tabular}
\end{table}

توجه به این نکته مهم است که مقادیر نسبت محافظت و ضریب جریمه در روش تمام‌شبکه، و نسبت‌های فراموشی و محافظت مشترک در روش آداپتری، همان کمیت‌هایی هستند که در فصل سوم به‌صورت نظری معرفی شدند. بنابراین میان صورت‌بندی ریاضی، تحلیل نظری و تنظیمات تجربی یک نگاشت مستقیم برقرار است و خواننده می‌تواند هر پارامتر عددی را به نقش آن در الگوریتم پیوند دهد.

\section{مجموعه‌داده‌ها}

سه محک تصویری برای ارزیابی استفاده شده است. \lr{CIFAR-10} \cite{krizhevsky2009learning} شامل ده کلاس تصویر رنگی کوچک است و در این پژوهش به پنج وظیفه‌ی دوکلاسه تقسیم می‌شود. این تنظیم ساده‌تر است و برای بررسی رفتار پایه‌ی روش‌ها و مقایسه‌ی دقیق میان روش‌های مختلف مناسب است. سطح تصادفی برای هر وظیفه‌ی دوکلاسه حدود $0.5$ است؛ بنابراین در \lr{CIFAR-10} اگر \lr{Au} پس از فراموشی به حدود $0.5$ برسد، یعنی مدل دیگر میان دو کلاس وظیفه‌ی حذف‌شده تمایز معناداری ندارد.

\lr{CIFAR-100} \cite{krizhevsky2009learning} محک دشوارتری است و در این پروژه با ده وظیفه‌ی پنج‌کلاسه استفاده شده است. طبق محدودیت پیاده‌سازی فعلی، هر وظیفه شامل پنج کلاس است و در تنظیم ده‌وظیفه‌ای، نخستین ۵۰ کلاس/ابرکلاس مورد استفاده قرار می‌گیرند. این تنظیم با بخشی از پروتکل \lr{PALL} سازگار است و به دلیل تعداد بیشتر وظایف، هم‌پوشانی و آسیب جانبی را بهتر آشکار می‌کند.

\lr{TinyImageNet} \cite{le2015tinyimagenet} محک بزرگ‌تر و پیچیده‌تری است که از تصاویر و کلاس‌های بیشتری نسبت به \lr{CIFAR} تشکیل شده است. در این پژوهش، تنظیم ۲۰ وظیفه با ۱۰ کلاس در هر وظیفه استفاده شده است. اجرای \lr{TinyImageNet} از نظر محاسباتی سنگین‌تر است و نتایج موجود برای آن در این نسخه بیشتر نقش صحت‌سنجی مقدماتی و بررسی مقیاس‌پذیری اولیه را دارند؛ بنابراین در تحلیل، با احتیاط بیشتری تفسیر می‌شوند.

\section{روش‌های مقایسه‌ای}

روش‌های مقایسه‌ای به سه گروه تقسیم می‌شوند. گروه نخست شامل روش‌های بازپخش مانند \lr{ER} و \lr{DER++} است. این روش‌ها در یادگیری پیوسته خط پایه‌های مهمی هستند و نشان می‌دهند حفظ دانش با کمک حافظه‌ی محدود تا چه حد ممکن است. با این حال، هدف اصلی آن‌ها حذف عمدی نیست و در مرحله‌ی فراموشی ممکن است یا دانش هدف را به‌خوبی حذف نکنند یا با رفتارهای خاصی مانند حفظ بیش از حد دانش گذشته روبه‌رو شوند.

گروه دوم شامل \lr{PALL-Original} است که خط پایه‌ی اصلی این پژوهش محسوب می‌شود. این روش وظایف را با سازوکار زیرشبکه‌ای یاد می‌گیرد و هنگام فراموشی مسیر مربوط به وظیفه‌ی هدف را حذف یا ترمیم می‌کند، اما هم‌پوشانی بحرانی را به‌طور صریح محافظت نمی‌کند. مقایسه با \lr{PALL-Original} نشان می‌دهد آیا ایده‌ی محافظت از ناحیه‌ی بحرانی واقعا ارزش افزوده دارد یا خیر.

گروه سوم شامل دو روش پیشنهادی است. \lr{PALL-Modified} برای بیشینه‌سازی دقت نگه‌داری طراحی شده و به همین دلیل اجازه دارد روی ظرفیت کامل شبکه و ماسک‌های زیرشبکه‌ای عمل کند. \lr{PALL-Adapter} برای کاهش هزینه و پایداری طراحی شده و بدنه‌ی شبکه را منجمد نگه می‌دارد. بنابراین انتظار نمی‌رود همیشه بیشترین دقت نهایی را داشته باشد؛ معیار اصلی موفقیت آن کاهش \lr{WorstDrop} و نسبت پارامترهای به‌روزرسانی‌شده است.

\section{معیارهای ارزیابی}

برای هر روش، چند معیار مکمل گزارش می‌شود:

\begin{itemize}
	\item \textbf{دقت میانگین نهایی:} میانگین دقت روی وظایف فعال پس از پایان دنباله‌ی درخواست‌ها. مقدار بیشتر بهتر است.
	\item \textbf{فراموشی میانگین:} معیار رایج یادگیری پیوسته که افت عملکرد نسبت به بهترین دقت گذشته را خلاصه می‌کند. مقدار کمتر بهتر است.
	\item \textbf{افت میانگین وظایف فعال ($\mathrm{Fu}$):} افت میانگین روی وظایف فعال در لحظه‌ی فراموشی. مقدار نزدیک صفر یا منفی نشان می‌دهد فراموشی به‌طور میانگین آسیب کمی زده است.
	\item \textbf{بیشترین افت ($\mathrm{WorstDrop}$):} بیشترین افت روی یک وظیفه‌ی فعال پس از درخواست فراموشی. این معیار مهم‌ترین شاخص پایداری است و مقدار کمتر بهتر است.
	\item \textbf{دقت وظیفه‌ی فراموش‌شده ($\mathrm{Au}$):} دقت روی وظیفه‌ی فراموش‌شده پس از حذف. این مقدار باید به سطح تصادفی نزدیک شود.
	\item \textbf{نسبت پارامتر به‌روزشده:} سهم پارامترهایی که در مرحله‌ی فراموشی تغییر می‌کنند. مقدار کمتر نشان‌دهنده‌ی هزینه‌ی پارامتری کمتر است.
\end{itemize}

نکته‌ی مهم در تفسیر \lr{WorstDrop} آن است که این معیار از میانگین سخت‌گیرانه‌تر است. ممکن است میانگین دقت وظایف فعال خوب باشد، اما یک وظیفه‌ی خاص به‌شدت آسیب ببیند. در کاربردهای واقعی، چنین آسیبی پذیرفتنی نیست؛ بنابراین \lr{WorstDrop} معیار اصلی برای سنجش آسیب جانبی در این پایان‌نامه است.

\section{نتایج اصلی روی روش‌های \lr{PALL}}

جدول \ref{tab:main-pall-results} نتایج اصلی سه روش \lr{PALL-Original}، \lr{PALL-Modified} و \lr{PALL-Adapter} را روی سه محک نشان می‌دهد. این جدول برای روشن‌کردن مصالحه‌ی اصلی پژوهش طراحی شده است: \lr{PALL-Modified} معمولا دقت نهایی بالاتری می‌دهد، در حالی که \lr{PALL-Adapter} نسبت پارامترهای به‌روزرسانی‌شده و \lr{WorstDrop} را کاهش می‌دهد.

\begin{table}[t]
\centering
\caption{نتایج اصلی روش‌های \lr{PALL} روی سه محک. مقدارهای \lr{TinyImageNet} تک‌بذر و از نوع صحت‌سنجی مقدماتی هستند.}
\label{tab:main-pall-results}
\resizebox{\columnwidth}{!}{
\begin{tabular}{llccccc}
\toprule
\textbf{داده} & \textbf{روش} & \textbf{دقت نهایی} & \textbf{فراموشی میانگین} & \textbf{\lr{WorstDrop}} & \textbf{\lr{Au}} & \textbf{نسبت به‌روزرسانی} \\
\midrule
\multirow{3}{*}{\lr{CIFAR-10}}
& \lr{PALL-Original} & $0.9166 \pm 0.0209$ & $0.1583 \pm 0.1217$ & $0.0160 \pm 0.0057$ & $0.5000 \pm 0.0000$ & $0.0475$ \\
& \lr{PALL-Modified} & $\mathbf{0.9301 \pm 0.0094}$ & $0.1507 \pm 0.1170$ & $\mathbf{0.0008 \pm 0.0011}$ & $0.5005 \pm 0.0007$ & $0.0475$ \\
& \lr{PALL-Adapter} & $0.8057 \pm 0.0054$ & $\mathbf{0.0916 \pm 0.0821}$ & $0.0050 \pm 0.0028$ & $\mathbf{0.5000 \pm 0.0000}$ & $\mathbf{0.0061}$ \\
\midrule
\multirow{3}{*}{\lr{CIFAR-100}}
& \lr{PALL-Original} & $0.4233 \pm 0.0297$ & $0.1611 \pm 0.0013$ & $0.0710 \pm 0.0212$ & $0.2250 \pm 0.0354$ & $0.0763$ \\
& \lr{PALL-Modified} & $\mathbf{0.5377 \pm 0.0299}$ & $0.1045 \pm 0.0267$ & $0.0140 \pm 0.0085$ & $0.2120 \pm 0.0170$ & $0.0763$ \\
& \lr{PALL-Adapter} & $0.4084 \pm 0.0293$ & $\mathbf{0.0853 \pm 0.0086}$ & $\mathbf{0.0050 \pm 0.0042}$ & $\mathbf{0.1640 \pm 0.0283}$ & $\mathbf{0.0123}$ \\
\midrule
\multirow{3}{*}{\lr{TinyImageNet}}
& \lr{PALL-Original} & $0.2485$ & $0.1446$ & $0.0620$ & $0.0920$ & $0.0961$ \\
& \lr{PALL-Modified} & $\mathbf{0.3302}$ & $0.0727$ & $0.0840$ & $0.1000$ & $0.0961$ \\
& \lr{PALL-Adapter} & $0.2874$ & $\mathbf{0.0641}$ & $\mathbf{0.0200}$ & $0.1120$ & $\mathbf{0.0292}$ \\
\bottomrule
\end{tabular}}
\end{table}

در \lr{CIFAR-10}، \lr{PALL-Modified} بهترین دقت نهایی را به‌دست می‌آورد: $0.9301$ در برابر $0.9166$ برای \lr{PALL-Original}. مهم‌تر از آن، \lr{WorstDrop} از $0.0160$ در نسخه‌ی اصلی به $0.0008$ در روش اصلاح‌شده کاهش می‌یابد. این کاهش نشان می‌دهد که محافظت از هم‌پوشانی بحرانی، آسیب بدترین وظیفه‌ی فعال را تقریبا حذف کرده است. \lr{PALL-Adapter} دقت نهایی پایین‌تری دارد، اما نسبت پارامترهای به‌روزرسانی‌شده را از $0.0475$ به $0.0061$ کاهش می‌دهد؛ یعنی حدود ۸۷ درصد کاهش نسبت به روش‌های تمام‌شبکه.

در \lr{CIFAR-100}، تفاوت‌ها آشکارتر می‌شوند. \lr{PALL-Original} با \lr{WorstDrop} برابر $0.0710$ آسیب قابل توجهی به دست‌کم یکی از وظایف فعال وارد می‌کند. \lr{PALL-Modified} این مقدار را به $0.0140$ کاهش می‌دهد و هم‌زمان دقت نهایی را از $0.4233$ به $0.5377$ افزایش می‌دهد. این نتیجه نشان می‌دهد که در تنظیم دشوارتر، محافظت صریح از ناحیه‌ی بحرانی نه‌تنها آسیب را کم می‌کند، بلکه به حفظ دقت کلی نیز کمک می‌کند. \lr{PALL-Adapter} کمترین \lr{WorstDrop} را دارد ($0.0050$) و نسبت به‌روزرسانی آن فقط $0.0123$ است، اما دقت نهایی آن کمتر از \lr{PALL-Modified} است. این همان مصالحه‌ی طراحی‌شده‌ی روش است: پایداری و کم‌هزینگی در برابر بیشینه‌سازی دقت.

در \lr{TinyImageNet} نیز الگوی کلی حفظ می‌شود، هرچند باید به تک‌بذر بودن نتایج توجه کرد. \lr{PALL-Modified} بیشترین دقت نهایی را دارد، اما \lr{PALL-Adapter} کمترین فراموشی میانگین، کمترین \lr{WorstDrop} و کمترین نسبت پارامترهای به‌روزرسانی‌شده را نشان می‌دهد. نسبت به‌روزرسانی از حدود $0.0961$ در روش‌های تمام‌شبکه به $0.0292$ در آداپتر کاهش یافته است، که نشان‌دهنده‌ی سود پارامتری در محک بزرگ‌تر است.

\subsection{تحلیل \lr{CIFAR-10}}

\lr{CIFAR-10} به دلیل تعداد کم کلاس‌ها و وظایف، محک مناسبی برای مشاهده‌ی رفتار پایه‌ی روش‌هاست. در این داده، هر وظیفه تنها دو کلاس دارد و سطح تصادفی برای وظیفه‌ی فراموش‌شده $0.5$ است. بنابراین تفسیر \lr{Au} ساده‌تر است: اگر پس از فراموشی، دقت روی وظیفه‌ی هدف نزدیک $0.5$ باشد، مدل دیگر میان دو کلاس آن وظیفه تمایز قابل اتکایی ندارد.

در این تنظیم، \lr{PALL-Modified} نشان می‌دهد محافظت از هم‌پوشانی بحرانی تقریبا بدون هزینه‌ی دقت انجام شده است. دقت نهایی آن از \lr{PALL-Original} بیشتر است و \lr{WorstDrop} آن بسیار کوچک‌تر. این نتیجه نشان می‌دهد در داده‌ی ساده‌تر، ناحیه‌ی بحرانی را می‌توان با تخمین گرادیانی به‌خوبی پیدا کرد و لنگرکردن آن در مرحله‌ی ترمیم، مانع آسیب به وظایف فعال می‌شود. از سوی دیگر، \lr{PALL-Adapter} دقت نهایی پایین‌تری دارد. علت محتمل این است که منجمدکردن بدنه و محدودکردن ظرفیت آموزش به آداپترهای کوچک، در تنظیم از صفر و بدون پیش‌آموزش، بخشی از ظرفیت لازم برای دقت بالا را کاهش می‌دهد. با این حال، هدف اصلی آداپتر یعنی کاهش پارامترهای به‌روزرسانی‌شده و کنترل \lr{WorstDrop} محقق شده است.

\subsection{تحلیل \lr{CIFAR-100}}

\lr{CIFAR-100} محک تعیین‌کننده‌تری برای پژوهش است، زیرا تعداد وظایف بیشتر و هر وظیفه پنج‌کلاسه است. در این حالت، احتمال هم‌پوشانی میان مسیرهای وظایف و حساسیت به فراموشی بیشتر می‌شود. نتایج نشان می‌دهد \lr{PALL-Original} در این محک آسیب جدی‌تری می‌بیند: \lr{WorstDrop} برابر $0.0710$ است، یعنی دست‌کم یک وظیفه‌ی فعال پس از فراموشی حدود هفت واحد درصد افت کرده است. این مقدار برای یک سامانه‌ی عملی بزرگ است، حتی اگر میانگین دقت قابل قبول به نظر برسد.

\lr{PALL-Modified} در همین تنظیم بهترین دقت نهایی را به‌دست می‌آورد. این نکته نشان می‌دهد محافظت از هم‌پوشانی صرفا مانع تخریب نیست، بلکه می‌تواند فرآیند ترمیم را مؤثرتر کند. وقتی وزن‌های بحرانی نزدیک مقدار قبلی نگه داشته می‌شوند، بازآموزی روی حافظه‌ی بازپخش وظایف فعال به جای بازسازی کامل دانش ازدست‌رفته، تنها اصلاحات محدود لازم را انجام می‌دهد. \lr{PALL-Adapter} کمترین \lr{WorstDrop} را دارد و \lr{Au} آن نیز نزدیک سطح تصادفی پنج‌کلاسه است. بنابراین اگر معیار اصلی پایداری و فراموشی باشد، آداپتر بهترین رفتار را نشان می‌دهد؛ اگر دقت نهایی اولویت بالاتری داشته باشد، نسخه‌ی اصلاح‌شده مناسب‌تر است.

\subsection{تحلیل \lr{TinyImageNet}}

\lr{TinyImageNet} از نظر اندازه و تنوع تصویری دشوارتر است. در این محک، دقت‌های مطلق پایین‌ترند و نوسان عملکرد می‌تواند بیشتر باشد. نتایج تک‌بذر نشان می‌دهد \lr{PALL-Modified} همچنان بیشترین دقت نهایی را دارد، اما \lr{WorstDrop} آن از \lr{PALL-Original} بیشتر گزارش شده است. این نکته نشان می‌دهد روش تمام‌شبکه در محک دشوارتر ممکن است به تنظیم دقیق‌تر نسبت محافظت، ضریب لنگر یا تعداد گام‌های ترمیم نیاز داشته باشد. در مقابل، \lr{PALL-Adapter} با وجود دقت نهایی متوسط، \lr{WorstDrop} کوچک‌تری دارد و نسبت به‌روزرسانی آن به‌مراتب کمتر است.

این نتیجه برای مسیر آینده مهم است. اگر هدف استقرار در داده‌های بزرگ‌تر باشد، پایداری ساختاری آداپتر و محدودبودن تغییرات پارامتری مزیت جدی دارد؛ اما برای رسیدن به دقت نهایی رقابتی، احتمالا باید بدنه‌ی منجمد از یک مدل پیش‌آموزش‌دیده‌ی قوی‌تر استفاده کند یا ظرفیت آداپترها افزایش یابد. مسیر توسعه‌ی پروژه نیز به‌سمت استفاده از بدنه‌ی پیش‌آموزش‌دیده و ذخیره‌سازی موقت ویژگی‌ها حرکت کرده است؛ این مسیر می‌تواند شکاف دقت آداپتر را کاهش دهد.

\section{مقایسه با روش‌های بازپخش}

جدول \ref{tab:baseline-results} روش‌های پیشنهادی را در کنار \lr{ER} و \lr{DER++} روی \lr{CIFAR-10} و \lr{CIFAR-100} نشان می‌دهد. این جدول نشان می‌دهد که روش‌های بازپخش، به‌ویژه \lr{DER++}، در حفظ دقت کلی قوی‌اند؛ اما معیارهای فراموشی و پایداری تصویر کامل‌تری ارائه می‌کنند.

\begin{table}[t]
\centering
\caption{مقایسه با روش‌های بازپخش روی \lr{CIFAR-10} و \lr{CIFAR-100}}
\label{tab:baseline-results}
\resizebox{\columnwidth}{!}{
\begin{tabular}{llccccc}
\toprule
\textbf{داده} & \textbf{روش} & \textbf{دقت نهایی} & \textbf{فراموشی میانگین} & \textbf{\lr{Fu}} & \textbf{\lr{WorstDrop}} & \textbf{\lr{Au}} \\
\midrule
\multirow{5}{*}{\lr{CIFAR-10}}
& \lr{DER++} & $0.9244 \pm 0.0167$ & $0.2399 \pm 0.1772$ & $0.0061$ & $0.0158$ & $0.2678$ \\
& \lr{ER} & $0.8864 \pm 0.0277$ & $0.2278 \pm 0.1534$ & $0.0307$ & $0.0557$ & $0.5318$ \\
& \lr{PALL-Original} & $0.9166 \pm 0.0209$ & $0.1583 \pm 0.1217$ & $0.0056$ & $0.0160$ & $0.5000$ \\
& \lr{PALL-Modified} & $\mathbf{0.9301 \pm 0.0094}$ & $0.1507 \pm 0.1170$ & $\mathbf{0.0004}$ & $\mathbf{0.0008}$ & $0.5005$ \\
& \lr{PALL-Adapter} & $0.8057 \pm 0.0054$ & $\mathbf{0.0916 \pm 0.0821}$ & $-0.0369$ & $0.0050$ & $0.5000$ \\
\midrule
\multirow{5}{*}{\lr{CIFAR-100}}
& \lr{DER++} & $0.5371 \pm 0.0097$ & $0.1040 \pm 0.0246$ & $0.0213$ & $0.0440$ & $0.3590$ \\
& \lr{ER} & $0.4657 \pm 0.0675$ & $0.1854 \pm 0.0368$ & $0.0734$ & $0.1400$ & $0.1870$ \\
& \lr{PALL-Original} & $0.4233 \pm 0.0297$ & $0.1611 \pm 0.0013$ & $0.0294$ & $0.0710$ & $0.2250$ \\
& \lr{PALL-Modified} & $\mathbf{0.5377 \pm 0.0299}$ & $0.1045 \pm 0.0267$ & $-0.0071$ & $0.0140$ & $0.2120$ \\
& \lr{PALL-Adapter} & $0.4084 \pm 0.0293$ & $\mathbf{0.0853 \pm 0.0086}$ & $-0.0020$ & $\mathbf{0.0050}$ & $\mathbf{0.1640}$ \\
\bottomrule
\end{tabular}}
\end{table}

در \lr{CIFAR-10}، \lr{DER++} دقت نهایی بالایی دارد، اما \lr{Au} آن برابر $0.2678$ است. چون سطح تصادفی برای وظیفه‌ی دوکلاسه حدود $0.5$ است، این مقدار نشان می‌دهد رفتار مدل روی وظیفه‌ی فراموش‌شده با تنظیم \lr{PALL} متفاوت است و تفسیر آن باید با توجه به پروتکل حذف انجام شود. \lr{ER} به \lr{Au} نزدیک‌تر به سطح تصادفی می‌رسد، اما \lr{WorstDrop} آن $0.0557$ است که از همه‌ی روش‌های \lr{PALL} بیشتر است. این یعنی حذف یا تغییر انجام‌شده به وظایف فعال آسیب بیشتری زده است.

در \lr{CIFAR-100}، \lr{DER++} و \lr{PALL-Modified} دقت نهایی تقریبا مشابهی دارند، اما \lr{WorstDrop} در \lr{PALL-Modified} حدود یک‌سوم \lr{DER++} است. \lr{PALL-Adapter} دقت نهایی پایین‌تری دارد، اما \lr{Au} و \lr{WorstDrop} آن بهتر از سایر روش‌هاست. این نتیجه نشان می‌دهد اگر هدف اصلی کنترل آسیب جانبی و حذف کم‌هزینه باشد، آداپتر گزینه‌ی مناسبی است؛ و اگر هدف بیشینه‌سازی دقت نهایی باشد، نسخه‌ی اصلاح‌شده‌ی تمام‌شبکه برتری دارد.

\section{تحلیل بیشترین افت}

شکل \ref{fig:worstdrop-results} مقایسه‌ی \lr{WorstDrop} را برای روش‌های \lr{PALL} نمایش می‌دهد. این شکل از داده‌های واقعی موجود در جدول‌های پروژه ساخته شده است و پیام اصلی آن با جدول \ref{tab:main-pall-results} سازگار است: روش‌های آگاه از هم‌پوشانی، بدترین افت را نسبت به \lr{PALL-Original} کاهش می‌دهند.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{worstdrop_chart.pdf}
    \caption[مقایسه بیشترین افت روش‌ها]{مقایسه‌ی \lr{WorstDrop} میان روش‌های \lr{PALL}. کاهش این معیار نشان‌دهنده‌ی کاهش آسیب به بدترین وظیفه‌ی فعال پس از فراموشی است.}
    \label{fig:worstdrop-results}
\end{figure}

از دید تحلیلی، این نتیجه با قضیه‌ی فصل سوم سازگار است. در \lr{PALL-Original}، ناحیه‌ی مشترک بدون محافظت کافی دستکاری می‌شود و بنابراین سهم ناحیه‌ی بحرانی در افت وظایف فعال کامل پرداخت می‌شود. در \lr{PALL-Modified}، جریمه‌ی لنگر روی $S_{\mathrm{crit}}$ مانع جابه‌جایی شدید وزن‌های مشترک مهم می‌شود. در \lr{PALL-Adapter}، ضرب‌گر ماسک نرم سهم همین ناحیه را با ضریب $1-p$ کاهش می‌دهد و بخشی از آن را سخت‌محافظت می‌کند. به همین دلیل، حتی وقتی دقت نهایی آداپتر پایین‌تر است، بدترین افت ناشی از درخواست فراموشی کوچک باقی می‌ماند.

\section{مصالحه‌ی دقت و هزینه‌ی پارامتری}

یکی از ادعاهای اصلی این پایان‌نامه آن است که هزینه‌ی پارامتری فراموشی را می‌توان بدون تخریب شدید پایداری کاهش داد. شکل \ref{fig:tradeoff-results} نسبت پارامترهای به‌روزرسانی‌شده را در برابر \lr{WorstDrop} نشان می‌دهد. نقطه‌ی مطلوب در این نمودار پایین و چپ است: هم پارامترهای کمتری تغییر کنند و هم افت بدترین وظیفه کم باشد.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{tradeoff_results.pdf}
    \caption[مصالحه هزینه پارامتری و بیشترین افت]{مصالحه‌ی نسبت پارامترهای به‌روزرسانی‌شده و \lr{WorstDrop}. \lr{PALL-Adapter} در ناحیه‌ی کم‌هزینه‌تر قرار می‌گیرد و هم‌زمان افت بدترین وظیفه را کوچک نگه می‌دارد.}
    \label{fig:tradeoff-results}
\end{figure}

در \lr{CIFAR-10}، نسبت به‌روزرسانی \lr{PALL-Adapter} برابر $0.0061$ است، در حالی که \lr{PALL-Original} و \lr{PALL-Modified} حدود $0.0475$ از پارامترها را درگیر می‌کنند. این کاهش برای سامانه‌هایی که با درخواست‌های حذف مکرر روبه‌رو هستند مهم است، زیرا هزینه‌ی اعمال تغییر، ذخیره‌ی نسخه‌ها و پایش اثرات جانبی را کاهش می‌دهد. در \lr{CIFAR-100} نیز نسبت از $0.0763$ به $0.0123$ کاهش می‌یابد. در \lr{TinyImageNet} کاهش از $0.0961$ به $0.0292$ است که اگرچه درصد کاهش آن کمتر از \lr{CIFAR-10} است، اما با توجه به دشواری محک همچنان معنادار است.

باید توجه داشت که پارامترکارآمد بودن به‌تنهایی کافی نیست. یک روش می‌تواند پارامترهای کمی را تغییر دهد اما فراموشی انجام ندهد، یا دقت وظایف فعال را خراب کند. مزیت \lr{PALL-Adapter} آن است که کاهش پارامتر با سازوکار هم‌پوشانی آگاه ترکیب شده است: آداپتر مخصوص وظیفه برای حذف دانش اختصاصی بازنشانی می‌شود و آداپتر مشترک با ماسک نرم کنترل می‌گردد.

\section{تحلیل آداپتر و محافظت بحرانی}

جدول \ref{tab:adapter-ablation} خلاصه‌ی مطالعه‌ی آداپتر روی \lr{CIFAR-100} را نشان می‌دهد. هدف این مطالعه بررسی نقش آداپتر مشترک و نسبت محافظت است. پیکربندی «بدون آداپتر مشترک» عملا دانش وظیفه‌ای را در آداپترهای جدا نگه می‌دارد و فراموشی را با بازنشانی انجام می‌دهد. پیکربندی «آداپتر مشترک بدون محافظت» اجازه می‌دهد بخشی از دانش میان وظایف مشترک شود، اما ناحیه‌ی بحرانی را کنترل نمی‌کند. پیکربندی‌های محافظت بحرانی با نسبت‌های مختلف، همین ناحیه را محدود می‌کنند.

\begin{table}[t]
\centering
\caption{خلاصه‌ی مطالعه‌ی آداپتر روی \lr{CIFAR-100}}
\label{tab:adapter-ablation}
\resizebox{\columnwidth}{!}{
\begin{tabular}{lccccc}
\toprule
\textbf{پیکربندی} & \textbf{دقت نهایی} & \textbf{فراموشی میانگین} & \textbf{\lr{WorstDrop}} & \textbf{\lr{Au}} & \textbf{نسبت به‌روزرسانی} \\
\midrule
بدون آداپتر مشترک & $0.3931 \pm 0.0109$ & $0.0648 \pm 0.0118$ & $0.0020 \pm 0.0000$ & $0.1840 \pm 0.0368$ & $0.0109$ \\
مشترک، بدون محافظت & $0.4203 \pm 0.0073$ & $0.0751 \pm 0.0063$ & $0.0060 \pm 0.0057$ & $0.1710 \pm 0.0382$ & $0.0123$ \\
محافظت بحرانی $p=0.05$ & $0.4186 \pm 0.0012$ & $0.0780 \pm 0.0054$ & $0.0090 \pm 0.0071$ & $0.1660 \pm 0.0170$ & $0.0123$ \\
محافظت بحرانی $p=0.10$ & $0.4194 \pm 0.0065$ & $0.0781 \pm 0.0033$ & $0.0070 \pm 0.0042$ & $0.1660 \pm 0.0368$ & $0.0123$ \\
محافظت بحرانی $p=0.20$ & $0.4084 \pm 0.0293$ & $0.0853 \pm 0.0086$ & $0.0050 \pm 0.0042$ & $0.1640 \pm 0.0283$ & $0.0123$ \\
\bottomrule
\end{tabular}}
\end{table}

این جدول چند پیام دارد. نخست، افزودن آداپتر مشترک می‌تواند دقت نهایی را نسبت به حالت بدون آداپتر مشترک افزایش دهد؛ زیرا بخشی از نمایش‌های مفید میان وظایف به اشتراک گذاشته می‌شوند. دوم، اشتراک بدون محافظت می‌تواند \lr{WorstDrop} را افزایش دهد، زیرا همان ناحیه‌ی مشترک منبع تداخل نیز هست. سوم، محافظت بحرانی با $p=0.20$ در این مجموعه نتایج کمترین \lr{WorstDrop} را میان پیکربندی‌های مشترک ایجاد می‌کند و هم‌زمان \lr{Au} را پایین‌تر نگه می‌دارد. این نشان می‌دهد محافظت شدیدتر می‌تواند فراموشی و پایداری را بهتر متوازن کند، هرچند ممکن است دقت نهایی اندکی کاهش یابد.

\section{رابطه‌ی هم‌پوشانی بحرانی و عملکرد}

برای بررسی مستقیم‌تر نقش هم‌پوشانی، مجموعه‌ای از اجراها با وظایف فراموش‌شونده‌ی متفاوت روی \lr{CIFAR-100} انجام شده است. در این اجراها، نسبت هم‌پوشانی بحرانی (سهم وزن‌های بحرانی از ناحیه‌ی مشترک) و معیارهایی مانند \lr{WorstDrop}، دقت نهایی و \lr{Au} ثبت شده‌اند. خلاصه‌ی همبستگی پیرسون در جدول \ref{tab:overlap-correlation} آمده است.

\begin{table}[t]
\centering
\caption{همبستگی نسبت هم‌پوشانی بحرانی با معیارهای عملکرد در اجراهای کاندید}
\label{tab:overlap-correlation}
\begin{tabular}{lcc}
\toprule
\textbf{معیار} & \textbf{همبستگی پیرسون} & \textbf{تعداد اجرا} \\
\midrule
\lr{WorstDrop} & $-0.5578$ & $10$ \\
فراموشی میانگین & $0.0071$ & $10$ \\
دقت نهایی & $0.0142$ & $10$ \\
\lr{Au} & $-0.0454$ & $10$ \\
\bottomrule
\end{tabular}
\end{table}

علامت منفی برای \lr{WorstDrop} در این مجموعه به این معنا نیست که هم‌پوشانی ذاتا مفید است؛ بلکه نشان می‌دهد در تنظیم محافظت‌شده‌ی آداپتر، اجراهایی که نسبت هم‌پوشانی بحرانی بالاتری داشته‌اند لزوما افت بیشتری نشان نداده‌اند. این نتیجه با فرضیه‌ی روش سازگار است: اگر ناحیه‌ی بحرانی شناسایی و محافظت شود، افزایش هم‌پوشانی نباید به‌صورت خطی و کنترل‌نشده به افزایش آسیب تبدیل شود. در عین حال، تعداد اجراها محدود است و برای نتیجه‌گیری آماری قوی‌تر، باید این تحلیل با بذرها و دنباله‌های بیشتری تکرار شود.

شکل \ref{fig:overlap-results} پراکندگی نسبت هم‌پوشانی بحرانی و دقت نهایی را نشان می‌دهد. این شکل بیشتر برای مشاهده‌ی روندها مفید است تا اثبات رابطه‌ی علی. با توجه به اینکه آداپتر مشترک به‌صورت ساختاری میان وظایف استفاده می‌شود، هم‌پوشانی یک کمیت ساده‌ی دوبه‌دو نیست؛ بلکه خلاصه‌ای از حساسیت گرادیانی در مسیر مشترک است.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{overlap_results.pdf}
    \caption[رابطه هم‌پوشانی بحرانی و دقت نهایی]{رابطه‌ی تجربی نسبت هم‌پوشانی بحرانی و دقت نهایی در اجراهای آداپتری.}
    \label{fig:overlap-results}
\end{figure}

\section{بحث درباره‌ی کیفیت فراموشی}

کیفیت فراموشی را نمی‌توان تنها با افت دقت وظیفه‌ی هدف سنجید، زیرا سطح تصادفی در مجموعه‌داده‌های مختلف متفاوت است. در \lr{CIFAR-10}، هر وظیفه دو کلاس دارد و سطح تصادفی تقریبا $0.5$ است. در این تنظیم، رسیدن \lr{Au} روش‌های \lr{PALL} به حدود $0.5$ نشان می‌دهد مدل دیگر تمایز معناداری میان کلاس‌های وظیفه‌ی حذف‌شده ندارد. در \lr{CIFAR-100}، هر وظیفه پنج کلاس دارد و سطح تصادفی حدود $0.2$ است؛ بنابراین مقادیر نزدیک به $0.2$ مطلوب‌اند. \lr{PALL-Adapter} با \lr{Au} برابر $0.1640$ و \lr{PALL-Modified} با $0.2120$ در همین ناحیه قرار دارند.

در \lr{TinyImageNet}، هر وظیفه ده کلاس دارد و سطح تصادفی حدود $0.1$ است. مقادیر \lr{Au} گزارش‌شده برای روش‌های \lr{PALL} بین $0.0920$ تا $0.1120$ قرار دارند؛ یعنی همگی به سطح تصادفی نزدیک‌اند. این نتیجه نشان می‌دهد حتی در محک دشوارتر، حذف وظیفه‌ی هدف از نظر دقت پس از فراموشی تا حد قابل قبولی انجام شده است. با این حال، چون نتایج \lr{TinyImageNet} تک‌بذر هستند، باید در نسخه‌ی نهایی پژوهش با اجرای چندبذری تکمیل شوند.

\section{بحث درباره‌ی زمان و هزینه‌ی فراموشی}

زمان کل فراموشی برای هر اجرا ثبت شده است. تفسیر این مقدار باید با احتیاط انجام شود، زیرا روش‌ها عملیات متفاوتی انجام می‌دهند. \lr{PALL-Modified} در \lr{CIFAR-100} به دلیل مرحله‌ی ترمیم تمام‌شبکه و محاسبه‌ی اهمیت، زمان بیشتری نسبت به \lr{PALL-Original} دارد. \lr{PALL-Adapter} با وجود به‌روزرسانی پارامترهای کمتر، ممکن است به دلیل حلقه‌ی تکراری فاز سوم و عملیات آداپتری، زمان مطلقی داشته باشد که همیشه از روش‌های ساده کمتر نیست. بنابراین مزیت اصلی آداپتر در این نسخه بیشتر در نسبت پارامترهای به‌روزرسانی‌شده و محدود بودن دامنه‌ی تغییرات است، نه الزاما کمترین زمان دیواری در همه‌ی اجراها.

برای استقرار واقعی، دو بهینه‌سازی می‌تواند زمان آداپتر را بهبود دهد. نخست، محاسبه‌ی ویژگی‌های بدنه‌ی منجمد را می‌توان یک‌بار انجام و ذخیره کرد تا در گام‌های بعد بازمحاسبه نشود؛ در روش‌های کارآمد پارامتری که بدنه ثابت است، این ذخیره‌سازی موقت ویژگی‌ها صرفه‌جویی محاسباتی چشمگیری دارد. دوم، حلقه‌ی فراموشی مشترک را می‌توان با تعداد گام کمتر یا توقف زودهنگام بر پایه‌ی دقت وظیفه‌ی فراموش‌شده تنظیم کرد. این موارد در کارهای آینده قابل توسعه‌اند.

\section{تهدیدهای اعتبار}

چند عامل می‌تواند اعتبار یا تعمیم‌پذیری نتایج را محدود کند. نخست، بخشی از نتایج چندبذری نیستند، به‌ویژه \lr{TinyImageNet}. برای ادعای نهایی درباره‌ی مقیاس‌پذیری، اجرای چند بذر و گزارش بازه‌ی اطمینان لازم است. دوم، تنظیم وظیفه‌آگاه ساده‌تر از تنظیم کلاس‌افزایشی است. اگر شناسه‌ی وظیفه در زمان آزمون در دسترس نباشد، مسئله دشوارتر می‌شود و روش باید با تشخیص وظیفه یا طبقه‌بند سراسری ترکیب گردد.

سوم، معیار \lr{Au} تنها شاخص غیرمستقیم حریم خصوصی است. پایین‌بودن دقت وظیفه‌ی فراموش‌شده نشانه‌ی خوبی است، اما تضمین نمی‌کند حمله‌ی استنتاج عضویت ناموفق باشد. برای تکمیل ادعای حریم خصوصی، باید حمله‌ی عضویت پس از فراموشی اجرا شود و موفقیت مهاجم قبل و بعد از حذف مقایسه گردد. چهارم، حافظه‌ی بازپخش خود می‌تواند منبع ریسک حریم خصوصی باشد؛ بنابراین در سامانه‌ی واقعی باید سیاست حذف نمونه‌های فراموش‌شده از حافظه و ثبت عملیات حذف به‌طور دقیق اجرا شود.

پنجم، نتایج به پیکربندی آداپتر حساس‌اند. نسبت‌های $\alpha_f$ و $\alpha_p$، اندازه‌ی گلوگاه، تعداد گام‌های فراموشی و شدت محافظت همگی می‌توانند روی تعادل میان دقت، فراموشی و پایداری اثر بگذارند. مطالعه‌ی ابلیشن انجام‌شده تصویر اولیه‌ای از این حساسیت می‌دهد، اما برای انتخاب خودکار ابرپارامترها کافی نیست.

\section{جمع‌بندی یافته‌ها در قالب پرسش‌های پژوهش}

پیش از جمع‌بندی نهایی، مفید است یافته‌های تجربی به‌صورت مستقیم به پرسش‌های پژوهش فصل نخست پیوند داده شوند. پرسش نخست درباره‌ی امکان شناسایی کم‌هزینه‌ی ناحیه‌ی مشترک مهم بود؛ نتایج نشان داد که تخمین مبتنی بر بزرگی گرادیان روی حافظه‌ی بازپخش برای این منظور کافی است و نیازی به محاسبه‌ی ماتریس فیشر کامل نیست. پرسش دوم درباره‌ی اثر محافظت بر بیشترین‌افت بود؛ کاهش چشمگیر این معیار در \lr{PALL-Modified} نسبت به \lr{PALL-Original}—به‌ویژه در \lr{CIFAR-10} که تقریبا به صفر رسید—پاسخ مثبت روشنی به این پرسش می‌دهد.

پرسش سوم درباره‌ی امکان کاهش هزینه‌ی پارامتری بدون ازدست‌رفتن پایداری بود؛ \lr{PALL-Adapter} با کاهش حدودا ۸۷درصدی نسبت پارامترهای به‌روزرسانی‌شده و هم‌زمان کوچک‌نگه‌داشتن بیشترین‌افت، نشان داد که این مصالحه دست‌یافتنی است، هرچند با اندکی کاهش در دقت نهایی همراه است. پرسش چهارم درباره‌ی رابطه‌ی تجربی هم‌پوشانی و آسیب بود؛ تحلیل همبستگی نشان داد که در تنظیم محافظت‌شده، افزایش هم‌پوشانی لزوما به افزایش کنترل‌نشده‌ی آسیب منجر نمی‌شود. پرسش پنجم درباره‌ی محدودیت‌های فراموشی تقریبی بود که در بخش تهدیدهای اعتبار و در فصل پایانی به‌تفصیل به آن پرداخته می‌شود.

\section{جمع‌بندی فصل}

نتایج تجربی نشان می‌دهد که کنترل صریح هم‌پوشانی بحرانی برای فراموشی انتخابی مفید است. \lr{PALL-Modified} در \lr{CIFAR-10} و \lr{CIFAR-100} بهترین دقت نهایی را میان روش‌های مقایسه‌شده به‌دست می‌آورد و \lr{WorstDrop} را نسبت به \lr{PALL-Original} به‌طور معنادار کاهش می‌دهد. \lr{PALL-Adapter} دقت نهایی پایین‌تری دارد، اما نسبت پارامترهای به‌روزرسانی‌شده را به‌شدت کاهش می‌دهد و در \lr{CIFAR-100} و \lr{TinyImageNet} کمترین \lr{WorstDrop} را به‌دست می‌آورد.

این یافته‌ها با تحلیل نظری فصل سوم هم‌راستا هستند: آسیب اصلی از ناحیه‌ای می‌آید که هم برای فراموشی و هم برای نگه‌داری مهم است؛ اگر این ناحیه شناسایی و محافظت شود، می‌توان فراموشی را بدون تخریب شدید وظایف فعال انجام داد. فصل بعد جمع‌بندی نهایی، محدودیت‌ها و مسیرهای آینده‌ی پژوهش را ارائه می‌کند.
