\chapter{آزمایش‌ها و نتایج}
\label{ch:results}

این فصل روش‌های فصل \ref{ch:proposed} را به‌طور تجربی ارزیابی می‌کند. ترتیب ارائه‌ی نتایج از پرسش‌های پژوهش بخش \ref{sec:intro-questions} پیروی می‌کند: نخست مقایسه‌ی اصلی هم‌تراز و تحلیل جفتی آن (\lr{RQ1} و \lr{RQ2})، سپس محک موقعیت درخواست (\lr{RQ3})، آنگاه کنترل‌های مؤلفه‌ای هر دو مسیر (\lr{RQ4} و \lr{RQ5})، و در پایان حسابداری هزینه، ممیزی حریم خصوصی و مطالعات حساسیت.

یک قاعده‌ی گزارش‌دهی در سراسر فصل رعایت می‌شود: هر جدول تعداد بذرهای خود را مشخص می‌کند، هیچ سلولی رتبه‌بندی نمی‌شود مگر آنکه معیار به‌تنهایی قابل خوانش باشد، و نتایج با کمتر از پنج بذر صریحا \emph{توصیفی} برچسب می‌خورند. مسیر بازتولید خروجی‌های تجمیعی و برچسب‌های آزمایش در پیوست مستند شده است.

\section{طراحی آزمایش}
\label{sec:res-design}

\subsection{محک‌ها و افراز وظیفه‌ها}
\label{subsec:res-benchmarks}

سه محک عمومی به‌کار می‌رود و افراز آن‌ها به وظیفه‌ها به‌گونه‌ای است که تعداد وظیفه و دشواری بازنمایی به‌تدریج افزایش یابد:

\begin{itemize}
	\item \lr{CIFAR-10} \cite{krizhevsky2009learning} به‌صورت $5$ وظیفه‌ی دوکلاسه؛ سطح تصادفی $0.5$.
	\item \lr{CIFAR-100} \cite{krizhevsky2009learning} در پروتکل هم‌پوشانی‌سنگین به‌صورت $10$ وظیفه‌ی پنج‌کلاسه (سطح تصادفی $0.2$) و در پروتکل استاندارد به‌صورت وظیفه‌های ده‌کلاسه (سطح تصادفی $0.1$).
	\item \lr{TinyImageNet} \cite{le2015tinyimagenet} به‌صورت $20$ وظیفه‌ی ده‌کلاسه؛ سطح تصادفی $0.1$.
\end{itemize}

\subsection{سه پروتکل مجزا}
\label{subsec:res-protocols}

نکته‌ی روش‌شناختی مهم این است که آزمایش‌ها در سه پروتکل \emph{مجزا} اجرا شده‌اند و نتایج آن‌ها با یکدیگر مخلوط نمی‌شوند. تنها استثنا جاروب پراکندگی بخش \ref{sec:res-overlap-direct} است که افراز و دنباله‌ی پروتکل استاندارد را با بودجه‌ی $3$ ایپاک ترکیب می‌کند؛ همان‌جا تصریح شده که اعداد آن تنها درون خود جدول قابل مقایسه‌اند. سه پروتکل چنین‌اند:

\begin{description}
	\item[پروتکل استاندارد.] $20$ ایپاک به‌ازای هر وظیفه، بدنه‌ی آموزش‌دیده از صفر، هشت بذر $\{0,\ldots,7\}$. این پروتکل، مبنای مقایسه‌ی اصلی بخش \ref{subsec:res-main} است.
	\item[پروتکل هم‌پوشانی‌سنگین.] $3$ ایپاک به‌ازای هر وظیفه و افراز پنج‌کلاسه‌ی \lr{CIFAR-100}. این پروتکل عمدا هم‌پوشانی بیشتری بین زیرشبکه‌ها ایجاد می‌کند و برای کنترل‌های مؤلفه‌ای \lr{EPALL} و مقایسه‌ی سه نسخه‌ی \lr{PALL} به‌کار می‌رود.
	\item[پروتکل پیش‌آموزش‌دیده.] بدنه‌ی \lr{ImageNet} \cite{deng2009imagenet} منجمد. این پروتکل، فرض طبیعی روش‌های کارآمد پارامتری را برقرار می‌کند و برای مقایسه‌ی \lr{LoRA} و \lr{PALL-Adapter} استفاده می‌شود.
\end{description}

در هر سه پروتکل، دنباله‌ی درخواست‌ها هر وظیفه را حداکثر یک‌بار آموزش می‌دهد، سه درخواست فراموشی دارد، و هیچ وظیفه‌ی حذف‌شده‌ای را دوباره معرفی نمی‌کند. درون هر بذر، همه‌ی روش‌ها \emph{دنباله‌ی درخواست یکسانی} می‌بینند؛ همین جفت‌شدگی است که تحلیل جفتی بخش \ref{subsec:res-paired} را ممکن می‌کند.

\subsection{معماری‌ها و ابرپارامترها}
\label{subsec:res-hyper}

به‌جز آزمون \lr{ViT}، اجراهای \lr{CIFAR-10}، \lr{TinyImageNet} و پیش‌آموزش‌دیده از نسخه‌های \lr{ResNet-18} و اجراهای اصلی \lr{CIFAR-100} از نسخه‌های \lr{ResNet-34} استفاده می‌کنند \cite{he2016deep}. بهینه‌ساز \lr{SGD} \cite{robbins1951sgd} با نرخ یادگیری $0.01$، تکانه‌ی $0.9$، تنظیم وزن $5\times10^{-4}$ و اندازه‌ی دسته‌ی $32$ است. بودجه‌ی بازپخش $500$ نمونه و از پیش میان وظایف افراز می‌شود، بدون بازتوزیع پس از حذف. تنها استثنا، \lr{LoRA} روی \lr{Split-CIFAR-100} است که نرخ یادگیری $10^{-3}$ می‌گیرد، چون با $10^{-2}$ واگرا می‌شود.

پیکربندی روش‌های پیشنهادی چنین است. \lr{EPALL}: نسبت محافظت $\rho=0.2$ و ضریب لنگر $\lambda=1$. \lr{PALL-Adapter}: گلوگاه وظیفه و گلوگاه مشترک $16$، $\alpha_f=0.3$، $\alpha_p=0.2$، $p=|\Scrit|/|\Sforget|$، ده گام فراموشی و پنجاه گام ترمیم. رتبه‌ی \lr{LoRA} برابر $8$. پراکندگی زیرشبکه $0.8$ روی \lr{CIFAR-10} و \lr{TinyImageNet} و $0.9$ روی \lr{CIFAR-100} است. پیکربندی کامل آزمایش‌ها در جدول \ref{tab:res-hyper} در پیوست آمده است.

\subsection{بذرها و سخت‌افزار}
\label{subsec:res-seeds}

مقایسه‌ی اصلی استاندارد، کنترل‌های مستقیم \lr{EPALL} و تحلیل لنگر از بذرهای $\{0,\ldots,7\}$ استفاده می‌کنند؛ جدول‌های پیش‌آموزش‌دیده، هم‌پوشانی‌سنگین و مؤلفه‌های آداپتر از $\{0,1,2\}$؛ و تحلیل‌های معیار اهمیت، \lr{ViT} و ماندگاری از $\{0,1\}$. محک موقعیت درخواست و جاروب پراکندگی هر دو پنج بذر $\{0,\ldots,4\}$ دارند و جاروب شدت ماسک پیوسته سه بذر. مرجع بازآموزی دو بذر، \lr{MIA} اصلاح‌شده سه بذر و حسابداری حافظه یک دنباله‌ی هم‌تراز دارد. جاروب عرض گلوگاه نیز دو بذر دارد و صرفا برای نشان‌دادن غیریکنوایی به‌کار می‌رود. پیکربندی سخت‌افزاری و نرم‌افزاری اجراها، به‌همراه نسخه‌ی مخزن و درهم‌سازی دنباله‌های درخواست، نیز در پیوست آمده است.

\section{معیارهای ارزیابی}
\label{sec:res-metrics}

پنج معیار گزارش می‌شود و تفسیر هر یک به‌دقت تعریف می‌شود.

\paragraph{دقت نهایی نگه‌داری $\Afinal$ (بیشتر بهتر).} میانگین دقت روی وظایف فعال در پایان دنباله.

\paragraph{فراموشی میانگین $\Favg$ (کمتر بهتر).} برای درخواست $r$:
\begin{equation}
	\label{eq:favg}
	\Favg(r)=\frac{1}{|\mathcal{T}_{\mathrm{trained}}|}\sum_{t}\Big[\max_{k<r}A_t(k)-A_t(r)\Big].
\end{equation}
این معیار روی همه‌ی وظایفی محاسبه می‌شود که تا لحظه‌ی $r$ آموزش دیده‌اند، از جمله وظایفی که بعدا حذف شده‌اند. به همین دلیل $\Favg$ وضعیت \emph{کل مسیر} یادگیری پیوسته را می‌سنجد و نه اثر یک درخواست مشخص را؛ برای سنجش اثر یک درخواست از دو معیار بعدی استفاده می‌شود.

\paragraph{افت نگه‌داری $F_u$ و بیشترین افت (کمتر بهتر).} $F_u$ اختلاف میانگین دقت وظایف نگه‌داری‌شده پیش و پس از یک درخواست است، و
\begin{equation}
	\label{eq:worstdrop}
	\mathrm{WorstDrop} = \max_{t \in \mathcal{T}_{a}} \big( A_{t}^{\mathrm{before}} - A_{t}^{\mathrm{after}} \big).
\end{equation}
این معیار \emph{علامت‌دار} گزارش می‌شود و در صفر بریده نمی‌شود؛ بنابراین مقدار منفی به این معنا است که ترمیم، بدترین وظیفه را بهبود داده است و این اطلاعات با بریدن از دست می‌رفت.

\paragraph{دقت وظیفه‌ی فراموش‌شده $A_u$.} بی‌درنگ پس از درخواست اندازه‌گیری می‌شود و همیشه از طریق $|A_u-c|$ خوانده می‌شود که $c$ سطح تصادفی همان وظیفه است. تأکید می‌شود که این معیار «کمتر بهتر» نیست.

\paragraph{زمان درخواست $T_f$ و نسبت دامنه‌ی ترمیم $\Rupd$.} $T_f$ زمان دیواری پردازش درخواست است و با \lr{\texttt{time.perf\_counter}} اندازه‌گیری می‌شود. چون فراخوانی‌های \lr{CUDA} ناهم‌گام‌اند و پیش از خواندن ساعت هیچ \lr{\texttt{torch.cuda.synchronize}} اجرا نمی‌شود، این عدد شامل هر کاری است که تا لحظه‌ی خواندن ساعت روی دستگاه هم‌گام شده باشد و نه لزوما تمام کار صف‌شده. بنابراین $T_f$ یک \emph{تشخیص} مرتبه‌ای است و نه یک اندازه‌گیری دقیق زمان محاسبه؛ همه‌ی نتیجه‌گیری‌های این پایان‌نامه درباره‌ی زمان، در همین سطح مرتبه‌ای بیان شده‌اند. $\Rupd$ \emph{تنها} نسبت اسمی پارامترهای آموزش‌پذیر در ماسک ترمیم نگه‌داری است؛ این معیار محاسبات کل، حافظه، زمان اجرا یا تعداد مختصه‌های واقعا تغییریافته را نمی‌سنجد. بخش \ref{sec:res-storage} به همین دلیل، حالت مقیم را جداگانه حسابداری می‌کند.

جدول‌ها از آخرین رویداد فراموشی هر اجرا استفاده می‌کنند و سپس میانگین و انحراف معیار نمونه‌ای را روی بذرها می‌گیرند.

\section{مقایسه‌ی اصلی استاندارد}
\label{subsec:res-main}

جدول‌های \ref{tab:res-main-c10} و \ref{tab:res-main-c100} مقایسه‌ی اصلی این پایان‌نامه‌اند. یازده روش، هشت بذر، و درون هر بذر یک دنباله‌ی درخواست مشترک. سطرها بر اساس \emph{فرض عملیاتی} گروه‌بندی شده‌اند و نه بر اساس عملکرد، چون مقایسه‌ی مستقیم روشی که یک شبکه به‌ازای هر وظیفه نگه می‌دارد با روشی که یک مدل مشترک دارد، بی‌معنا است. \lr{SSD} و \lr{SalUn} از تجمیع هم‌پروتکل خود می‌آیند؛ اجراهای تک‌بذری تنظیم \lr{SSD} در این مقایسه وارد نشده‌اند.

\input{tables/res_main_cifar10}

\input{tables/res_main_cifar100}

\subsection{خوانش نتایج اصلی}
\label{subsec:res-main-read}

\paragraph{\lr{CLPU} قوی‌ترین سطر است و ما ادعای برتری بر آن نداریم.} پیاده‌سازی ارزیابی‌شده یک شبکه‌ی کامل به‌ازای هر وظیفه‌ی فعال نگه می‌دارد و فراموشی را با پاک‌کردن همان مدخل انجام می‌دهد. بنابراین زمان درخواست زیرمیلی‌ثانیه‌ای آن، هزینه‌ی یک حذف از دیکشنری را می‌سنجد و نه هزینه‌ی نگه‌داشتن آن شبکه‌ها را. \lr{CLPU} وقتی چنین جداسازی‌ای قابل قبول باشد گزینه‌ی جذابی است، اما یک راه‌حل مدل‌مشترک نیست. بخش \ref{sec:res-storage} هزینه‌ی پنهان آن را آشکار می‌کند.

\paragraph{میان روش‌های مدل‌مشترک، \lr{EPALL} به مرجع جداسازی نزدیک می‌شود.} روی \lr{CIFAR-10} فاصله‌ی $\Afinal$ آن از \lr{CLPU} کمتر از یک واحد درصد است ($0.9433$ در برابر $0.9500$) و روی \lr{CIFAR-100} دو مقدار عددا نزدیک‌اند ($0.7371$ در برابر $0.7355$). این نزدیکی نه برتری است و نه هم‌ارزی اثبات‌شده: اختلاف نسبت به پراکندگی بذرها کوچک است و هیچ آزمون هم‌ارزی برای آن اجرا نشده است. هم‌زمان $A_u$ را به سطح تصادفی می‌رساند ($0.4863$ و $0.0959$) و بیشترین افت را در $0.0027$ و $0.0056$ نگه می‌دارد. بنابراین بدون دادن یک شبکه‌ی کامل به هر وظیفه، عملا به همان نقطه می‌رسد.

\paragraph{ستون $A_u$ روش‌های حفظ‌محور را رد می‌کند.} \lr{EWC} و \lr{LwF} روی \lr{CIFAR-10} بیشترین افت صفر ثبت می‌کنند — که در نگاه اول عالی است — اما $A_u$ آن‌ها $0.8122$ و $0.9469$ است؛ یعنی وظیفه‌ی هدف عملا حذف نشده و بخش عمده‌ی رفتار آن حفظ شده است. به همین ترتیب، \lr{SSD} روی \lr{CIFAR-10} با $A_u=0.8453$ می‌ماند و هر دو روش \lr{SSD} و \lr{SalUn} روی \lr{CIFAR-100} با $0.2619$ و $0.3967$ به‌روشنی بالای هدف $0.1$ باقی می‌مانند. این دقیقا نمونه‌ای است که چرا معیارها باید هم‌زمان خوانده شوند: هیچ‌یک از این دو ستون به‌تنهایی داوری درستی نمی‌دهد.

\paragraph{دو سطر \lr{PEFT} تشخیصی‌اند و رقیب هم‌ظرفیت نیستند.} در این پروتکل از صفر، هم \lr{LoRA} و هم \lr{PALL-Adapter} روی یک \lr{ResNet} منجمد با مقدارگذاری تصادفی نشسته‌اند. دقت \lr{CIFAR-100} آن‌ها به‌طور مشابه پایین است ($0.2695$ و $0.2583$) به‌رغم آنکه سازوکار فراموشی‌شان متفاوت است، و هر دو با پیش‌آموزش‌دیده‌شدن بدنه بازیابی می‌شوند (جدول \ref{tab:res-pretrained}). این الگو به یک گلوگاه \emph{بازنمایی} اشاره می‌کند و نه به گام فراموشی.

شکل \ref{fig:res-main-metrics-accuracy} دقت نهایی نگه‌داری، شکل \ref{fig:res-main-metrics-worstdrop} بیشترین افت و شکل \ref{fig:res-main-metrics-au} دقت وظیفه‌ی فراموش‌شده را برای هر سه مجموعه‌داده و هر رژیم موجود نشان می‌دهند. جداسازی هر معیار در یک صفحه‌ی افقی، خوانایی برچسب‌ها و بازه‌های اطمینان را در نسخه‌ی چاپی حفظ می‌کند.

\begin{landscape}
\begin{figure}[p]
	\centering
	\captionsetup{font=footnotesize}
	\includegraphics[width=0.98\linewidth]{main_metrics_final_accuracy_1x3.pdf}
	\caption[دقت نهایی به تفکیک مجموعه‌داده و رژیم]{دقت نهایی به تفکیک مجموعه‌داده و رژیم. رژیم‌ها تجمیع نشده‌اند؛ میله‌ها میانگین جدول و خطاها بازه‌ی بوت‌استرپ ۹۵ درصد بذرهای اجراهای کامل‌شده هستند.}
	\label{fig:res-main-metrics-accuracy}
\end{figure}
\end{landscape}

\begin{landscape}
\begin{figure}[p]
	\centering
	\captionsetup{font=footnotesize}
	\includegraphics[width=0.98\linewidth]{main_metrics_worstdrop_1x3.pdf}
	\caption[بیشترین افت به تفکیک مجموعه‌داده و رژیم]{بیشترین افت علامت‌دار به تفکیک مجموعه‌داده و رژیم. رژیم‌ها تجمیع نشده‌اند؛ خط عمودی صفر حفظ شده و مقادیر منفی، بهبود پس از ترمیم را نشان می‌دهند.}
	\label{fig:res-main-metrics-worstdrop}
\end{figure}
\end{landscape}

\begin{landscape}
\begin{figure}[p]
	\centering
	\captionsetup{font=footnotesize}
	\includegraphics[width=0.98\linewidth]{main_metrics_au_1x3.pdf}
	\caption[دقت وظیفه‌ی حذف‌شده به تفکیک مجموعه‌داده و رژیم]{$A_u$ به تفکیک مجموعه‌داده و رژیم. رژیم‌ها تجمیع نشده‌اند؛ خط‌چین سطح تصادفی هر مجموعه‌داده را نشان می‌دهد.}
	\label{fig:res-main-metrics-au}
\end{figure}
\end{landscape}

\subsection{تحلیل جفتی و آزمون معناداری}
\label{subsec:res-paired}

چون دنباله‌ها بر اساس بذر جفت شده‌اند، مقایسه‌ی مستقیم \lr{EPALL} با \lr{PALL} اصلی ممکن است. جدول \ref{tab:res-paired} اختلاف‌های جفتی و بازه‌های بوت‌استرپ توصیفی را نشان می‌دهد.

\input{tables/res_paired}

روی \lr{CIFAR-100} اختلاف $\Afinal$ مثبت و برابر $+0.0147$ با بازه‌ی $[0.0062,0.0229]$ است، با بهبود میانگین $0.0110$ در $\Favg$ و $0.0166$ در بیشترین افت؛ هیچ‌یک از این سه بازه صفر را در بر نمی‌گیرد. روی \lr{CIFAR-10} افزایش دقت $+0.0087$ $[0.0034,0.0151]$ و بهبود بیشترین افت $0.0092$ $[0.0029,0.0157]$ است، در حالی که $\Favg$ عملا مختلط است ($-0.0017$ با بازه‌ای که صفر را در بر می‌گیرد). هزینه‌ی این نگه‌داری، زمان درخواست است: \lr{EPALL} حدود $2.10$ ثانیه روی \lr{CIFAR-10} و $12.06$ ثانیه روی \lr{CIFAR-100} از \lr{PALL} اصلی کندتر است.

جدول \ref{tab:res-significance} آزمون‌های دقیق یک‌طرفه را گزارش می‌کند. چون شش آزمون هم‌زمان انجام می‌شود، مقدار $p$ خام به‌تنهایی کافی نیست و اصلاح \lr{Holm} برای چندگانگی اعمال شده است. روی \lr{CIFAR-100} هر سه معیار به $p=0.007812$ می‌رسند و هفت جفت از هشت جفت به سود \lr{EPALL} است، با اندازه‌های اثر جفتی $d_z$ برابر $1.11$، $1.51$ و $1.35$؛ هر سه پس از اصلاح \lr{Holm} نیز در سطح $0.05$ باقی می‌مانند. روی \lr{CIFAR-10} تنها $\Afinal$ باقی می‌ماند ($p=0.007812$ و پس از \lr{Holm} برابر $0.0469$).

\input{tables/res_significance}

تفسیر این جدول باید محتاطانه بماند و دو تصحیح مهم دارد. نخست، \textbf{بیشترین افت روی \lr{CIFAR-10} پس از اصلاح \lr{Holm} معنادار نیست}: مقدار خام $0.03125$ پس از اصلاح به $0.0625$ می‌رسد و از سطح $0.05$ می‌گذرد. آزمون علامت همان معیار نیز به $p=0.109$ می‌رسد، پس شاهد آن از دو جهت ضعیف است و در این پایان‌نامه به‌عنوان یافته‌ی معنادار گزارش نمی‌شود. دوم، \textbf{کف $p$ قابل‌دستیابی برای همه‌ی سطرها یکسان نیست}: این کف به تعداد جفت‌های بی‌گره بستگی دارد و برای سطرهای \lr{CIFAR-100} (بدون گره) برابر $0.003906$ است، نه $0.007812$. بنابراین مقادیر مشاهده‌شده‌ی \lr{CIFAR-100} \emph{روی کف نایستاده‌اند} بلکه دو برابر آن هستند، و این یعنی توان آزمون کاملا اشباع نشده است. تنها معیاری که در هیچ مجموعه‌داده‌ای بهبود پیوسته نشان نمی‌دهد $\Favg$ روی \lr{CIFAR-10} است، که با علامت منفی اختلاف جفتی در جدول \ref{tab:res-paired} سازگار است.

جمع‌بندی محافظه‌کارانه: از شش آزمون، \textbf{چهار آزمون} پس از اصلاح چندگانگی باقی می‌مانند — هر سه معیار روی \lr{CIFAR-100} و دقت نهایی روی \lr{CIFAR-10}.

\paragraph{یادداشت درباره‌ی تعداد بذر.} نتایج این بخش بر هشت بذر ($0$ تا $7$) استوارند که وضعیت کامل‌شده‌ی مخزن نتایج است. نسخه‌ی مقاله‌ی کنفرانسی همین کار بر پنج بذر ($0$ تا $4$) بسته شده بود و اعداد آن با اعداد این‌جا تفاوت جزئی دارد؛ جهت همه‌ی نتیجه‌گیری‌ها یکسان مانده است، اما مقادیر عددی و $p$ها متفاوت‌اند. هرجا در این پایان‌نامه عددی گزارش می‌شود، منبع آن وضعیت کنونی مخزن نتایج است و نه مقاله.

\section{محک موقعیت درخواست}
\label{sec:res-overlap-response}

ادعای انگیزشی \lr{EPALL} این بود که محافظت \emph{با رشد هم‌پوشانی} اهمیت بیشتری می‌یابد. برای آزمون این ادعا یک محک با پنج درجه‌ی از پیش تعیین‌شده ساخته شد: پنج درجه $\times$ شش روش $\times$ دو مجموعه‌داده $\times$ پنج بذر $=300$ اجرای هم‌تراز.

\textbf{اما باید تصریح شود که این محک، هم‌پوشانی را کنترل نمی‌کند.} آنچه درجه‌ها واقعا تغییر می‌دهند، \emph{موقعیت درخواست فراموشی در دنباله} است؛ یعنی تعداد وظایفی که پس از وظیفه‌ی هدف آموزش می‌بینند. این کمیت را $K$ می‌نامیم: روی \lr{CIFAR-10} برابر $K\in\{0,1,2,3,4\}$ و روی \lr{CIFAR-100} برابر $K\in\{0,2,4,7,9\}$ است. چون افراز کلاس‌ها و شناسه‌های وظیفه در این خط لوله ثابت‌اند، کنترل هم‌پوشانی \emph{واقعی} ماسک‌ها به تغییر خود افراز داده نیاز دارد که در این کار انجام نشده است.

در نتیجه $K$ تنها یک \emph{شاخص جانشین} برای هم‌پوشانی مورد انتظار است و با تغییر آن، چند عامل هم‌زمان جابه‌جا می‌شوند: هویت وظیفه‌ی هدف، سن آن وظیفه در لحظه‌ی حذف، ترتیب درخواست‌ها، و مقدار آموزشی که پس از هدف انجام می‌شود. بنابراین این محک را در سراسر پایان‌نامه \textbf{محک موقعیت درخواست} می‌نامیم و نتیجه‌ی آن را یک \emph{روند توصیفی سازگار با فرضیه} می‌خوانیم، نه شاهد علّی. جدول \ref{tab:res-overlap-levels} بیشترین افت را در همه‌ی این خانه‌ها گزارش می‌کند.

\input{tables/res_overlap_levels}

الگوی \lr{CIFAR-100} همان چیزی است که سازوکار پیش‌بینی می‌کرد: بیشترین افت \lr{PALL} اصلی با افزایش شاخص موقعیت درخواست از $0.0124$ به $0.0456$ رشد می‌کند، در حالی که \lr{EPALL} در بازه‌ی $0.0060$ تا $0.0164$ نسبتا مسطح می‌ماند. روی \lr{CIFAR-10} همین ترتیب حفظ می‌شود اما در بازه‌ای کوچک‌تر. \lr{SalUn} رفتاری مشابه \lr{PALL} اصلی نشان می‌دهد و \lr{CLPU} همان‌گونه که انتظار می‌رفت به‌طور ساختاری صفر می‌ماند، چون شبکه‌های مستقل امکان تداخل ندارند. شکل \ref{fig:res-overlap-response} همین روند را نمایش می‌دهد.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.82\columnwidth]{aaai_overlap_response.pdf}
	\caption[پاسخ بیشترین افت به موقعیت درخواست]{بیشترین افت در پنج درجه‌ی موقعیت درخواست برای چهار روش اصلیِ نمایش‌داده‌شده (میانگین پنج اجرای هم‌تراز هر درجه؛ کمتر بهتر). درجه‌های محور افقی از «بسیار کم» تا «بسیار زیاد»، تعداد وظایف آموزش‌دیده پس از وظیفه‌ی هدف را کدگذاری می‌کنند و شاخصی جانشین برای هم‌پوشانی مورد انتظارند، نه خود هم‌پوشانی. \lr{PALL} اصلی با افزایش این شاخص بالا می‌رود، در حالی که \lr{EPALL} نسبتا مسطح می‌ماند؛ \lr{CLPU} به‌طور ساختاری صفر است. دو روش دیگر در جدول \ref{tab:res-overlap-levels} آمده‌اند.}
	\label{fig:res-overlap-response}
\end{figure}

\paragraph{هشدار روش‌شناختی.} این یک روند \emph{درجه‌به‌درجه} و توصیفی است و هیچ ادعای علّی از آن بیرون نمی‌آید، چون درجه هم‌زمان چند عامل را جابه‌جا می‌کند. هم‌پوشانی \emph{محقق‌شده} میان روش‌ها متفاوت است و رگرسیون‌های تجمیعی روی هم‌پوشانی اندازه‌گیری‌شده عمدتا صفر را در بر می‌گیرند. جدول \ref{tab:res-slopes} این رگرسیون‌ها را گزارش می‌کند: تنها استثنا شیب \lr{CIFAR-100} مسیر آداپتر است. توجه شود که سنجه‌ی محور افقی برای دو خانواده یکسان نیست — برای \lr{PALL} کامل، میانگین اشتراک-بر-اجتماع ماسک‌های زیرشبکه و برای آداپتر، نسبت بحرانی مشترک — پس مقایسه‌ی بزرگی شیب‌ها میان این دو بازنمایی نیازمند احتیاط است.

\input{tables/res_slopes}

بازه‌های بسیار پهن سطرهای \lr{PALL} کامل، پیامد مستقیم آن است که هم‌پوشانی اندازه‌گیری‌شده در آن اجراها تنها سه یا چهار مقدار یگانه دارد؛ بنابراین این جدول را باید نشانه‌ی \emph{کمبود تنوع در متغیر مستقل} خواند و نه شاهد نبود اثر. همین محدودیت، دلیل اصلی طراحی محک سطح‌محور بود.

\section{جاروب پراکندگی و هم‌پوشانی اندازه‌گیری‌شده}
\label{sec:res-overlap-direct}

محک بخش \ref{sec:res-overlap-response} یک ضعف روش‌شناختی دارد که در همان‌جا تصریح شد: درجه‌ها \emph{موقعیت درخواست} را تغییر می‌دهند و هم‌پوشانی تنها به‌عنوان یک اثر جانبی جابه‌جا می‌شود؛ هم‌زمان هویت وظیفه‌ی هدف، سن آن و مقدار آموزش پس از آن نیز تغییر می‌کنند. بنابراین آن محک با فرضیه سازگار است اما آن را نمی‌آزماید. این بخش یک آزمون مکمل ارائه می‌کند که در آن موقعیت درخواست ثابت است و دستگیره‌ی آزمایشی، پراکندگی زیرشبکه است. چون این دستگیره ظرفیت را نیز تغییر می‌دهد، نباید آن را «دستکاری مستقیم و خالص هم‌پوشانی» نامید.

\paragraph{طرح.} دنباله‌ی درخواست، وظیفه‌ی هدف و موقعیت آن دقیقا ثابت نگه داشته می‌شوند و تنها \emph{پراکندگی زیرشبکه} تغییر می‌کند. پراکندگی تعیین می‌کند هر ماسک وظیفه چند مختصه بگیرد و در نتیجه با $|\Sshare|=|\Mf\cap\Mr|$ ارتباط مستقیم دارد، اما هم‌زمان ظرفیت هر وظیفه را نیز تغییر می‌دهد. پنج سطح پراکندگی از $0.9$ تا $0.5$، دو روش و پنج بذر، در مجموع $100$ اجرای هم‌تراز می‌سازند. این جاروب افراز و دنباله‌ی پروتکل استاندارد را به‌کار می‌برد، اما با بودجه‌ی $3$ ایپاک به‌ازای هر وظیفه اجرا شده است و نه $20$ ایپاک. بنابراین سطح مطلق دقت آن با جدول‌های بخش \ref{subsec:res-main} قابل مقایسه نیست و تنها مقایسه‌ی \emph{درون‌جدولی} دو بازو در هر سطح پراکندگی معتبر است. دو بازوی هر خانه پراکندگی و بذر یکسان می‌بینند؛ هم‌پوشانی محقق‌شده‌ی آن‌ها کاملا برابر نیست، ازاین‌رو میانگین جفت گزارش می‌شود. بیشترین اختلاف نسبی دو بازو کمتر از $0.05\%$ است.

\paragraph{محک جاروب.} برخلاف محک موقعیت درخواست که متغیر مستقلش تنها سه یا چهار مقدار یگانه داشت، اینجا هم‌پوشانی اندازه‌گیری‌شده در بازه‌ای $19.7$ برابری روی \lr{CIFAR-10} و $10.7$ برابری روی \lr{CIFAR-100} حرکت می‌کند و ترتیب آن با سطوح پراکندگی یکنواست. این مشاهده نشان می‌دهد دستگیره‌ی پراکندگی، دامنه‌ی کافی در هم‌پوشانی ایجاد کرده است؛ بااین‌حال اثر هم‌پوشانی را از اثر ظرفیت جدا نمی‌کند.

\input{tables/res_overlap_sparsity}

جدول \ref{tab:res-overlap-sparsity} نتیجه را نشان می‌دهد. روی \lr{CIFAR-10} سود حفاظت از $0.0018$ در کم‌ترین هم‌پوشانی به $0.0349$ در بیش‌ترین می‌رسد. روی \lr{CIFAR-100} از $0.0146$ آغاز می‌شود، در سطح پراکندگی $0.6$ به بیشینه‌ی $0.1002$ می‌رسد و در بیشترین هم‌پوشانی $0.0794$ است؛ بنابراین روند مثبت است اما یکنوا نیست. همبستگی رتبه‌ای اسپیرمن میان هم‌پوشانی اندازه‌گیری‌شده و سود حفاظت روی \lr{CIFAR-10} برابر $+1.00$ و روی \lr{CIFAR-100} برابر $+0.90$ است. چون تنها پنج سطح در کار است، به‌جای تقریب مجانبی، هر $120$ جایگشت ممکن شمرده شده و $p$ دقیق به‌دست آمده است: به‌ترتیب $0.0083$ و $0.0417$. برای سود دقت نهایی، همبستگی روی هر دو مجموعه‌داده $+1.00$ و $p$ دقیق $0.0083$ است.

\paragraph{دو قید.} نخست، پراکندگی علاوه بر هم‌پوشانی، \emph{ظرفیت} را هم تغییر می‌دهد و این مخدوش‌کننده با طراحی حذف‌شدنی نیست: دقت \lr{PALL} در همین جاروب روی \lr{CIFAR-100} از $0.4919$ به $0.3292$ می‌افتد. همسان‌بودن پراکندگی اسمی در هر جفت، مقایسه‌ی دو روش را تفسیرپذیرتر می‌کند، اما اثر علّی مستقل هم‌پوشانی را شناسایی نمی‌کند. دوم، آزمون علامت بذری درون هر خانه با پنج بذر توان کمی دارد و تنها سه خانه از ده خانه به $p<0.05$ می‌رسند؛ میانگین‌ها اختلاف بزرگی نشان می‌دهند اما پراکندگی بذرها بالاست. بنابراین شاهد اصلی این بخش در روند میان سطوح است و نه در تک‌تک خانه‌ها.

\paragraph{جمع‌بندی.} با ثابت‌نگه‌داشتن هویت و موقعیت وظیفه‌ی هدف، جاروب پراکندگی یک رابطه‌ی رتبه‌ای مثبت میان هم‌پوشانی اندازه‌گیری‌شده و سود حفاظت نشان می‌دهد. این نتیجه از ادعای انگیزشی \lr{EPALL} پشتیبانی می‌کند، اما به‌دلیل تغییر هم‌زمان ظرفیت و غیریکنوایی \lr{CIFAR-100} اثبات علّی خالصِ اثر هم‌پوشانی نیست.

\section{انتساب سازوکار \lr{EPALL}}
\label{subsec:res-components}

پرسش \lr{RQ4} می‌پرسد کدام جزء سازوکار واقعا منشأ بهبود است. جدول \ref{tab:res-components} پنج بازوی هم‌تراز را نشان می‌دهد که پروتکل هم‌پوشانی‌سنگین، دنباله‌ها و بذرها را به اشتراک می‌گذارند و هر یک تنها در یک جزء تفاوت دارند؛ تعریف این بازوها در بخش \ref{subsec:prop-controls} آمد.

\input{tables/res_components}

جدول \ref{tab:res-components-paired} اختلاف‌های جفتی نسبت به بازوی کامل را می‌دهد و خوانش دقیق‌تری ممکن می‌سازد.

\input{tables/res_components_paired}

خوانش این دو جدول چهار نتیجه می‌دهد. دو نتیجه به سود سازوکار پیشنهادی است و دو نتیجه نیست؛ هر چهار به‌صراحت گزارش می‌شوند.

\paragraph{یک. کیفیت انتخاب مهم است، نه فقط اندازه‌ی بودجه.} این روشن‌ترین و پایدارترین یافته‌ی کنترل‌ها است و روی \emph{هر دو} مجموعه‌داده برقرار می‌ماند. بازوی بودجه‌ی تصادفی همان تعداد مختصه را محافظت می‌کند، اما بیشترین افت را $0.0076$ روی \lr{CIFAR-10} و $0.0148$ روی \lr{CIFAR-100} بدتر می‌کند و هر دو بازه صفر را در بر نمی‌گیرند؛ روی \lr{CIFAR-10} اختلاف دقت نیز ($+0.0027$) از صفر جدا است. این مقایسه دقیقا همان چیزی را جدا می‌کند که باید: با ثابت‌نگه‌داشتن اندازه‌ی بودجه، تفاوت باقی‌مانده تنها می‌تواند از کیفیت رتبه‌بندی بیاید.

\paragraph{دو. رتبه‌بندی نسبت به محافظت بدون رتبه، نگه‌داری بهتری می‌دهد اما همه‌ی معیارها را غالب نمی‌کند.} روی \lr{CIFAR-100} بازوی کامل دقت جفتی را نسبت به فقط‌هم‌پوشانی $0.0120$ $[0.0065,0.0191]$ بهبود می‌دهد. اما بازوی فقط‌هم‌پوشانی بیشترین افت \emph{کمتری} دارد، و این بار روی هر دو مجموعه‌داده: $0.0022$ روی \lr{CIFAR-10} و $0.0075$ روی \lr{CIFAR-100}، هر دو با بازه‌ای که صفر را در بر نمی‌گیرد. یعنی رتبه‌بندی، پایداری بی‌درنگ بدترین وظیفه را در ازای نگه‌داری نهایی بالاتر معاوضه می‌کند. این یک مصالحه‌ی واقعی است و نه یک برتری یکنواخت؛ اگر معیار عملیاتی یک سامانه بدترین وظیفه باشد، محافظت بدون رتبه‌بندی انتخاب بهتری است.

\paragraph{سه. سهم مستقل جریمه‌ی $\ell_2$ پشتیبانی نمی‌شود.} این بازو تنها جمله‌ی جریمه را حذف می‌کند؛ شناسایی ناحیه‌ی بحرانی و ساخت پشتیبان‌های آن دست‌نخورده باقی می‌ماند، پس نتیجه‌ی آن درباره‌ی \emph{شدت مهار} است و نه درباره‌ی کل محافظت. این بازو روی هیچ‌یک از دو مجموعه‌داده به‌روشنی از بازوی کامل جدا نمی‌شود: اختلاف دقت جفتی روی \lr{CIFAR-100} برابر $0.0052$ با بازه‌ی $[-0.0008,0.0129]$ است که صفر را در بر می‌گیرد، و روی \lr{CIFAR-10} برآورد نقطه‌ای $-0.0048$ است، یعنی حتی کمی به سود \emph{حذف} لنگر. اختلاف بیشترین افت در هر دو مورد عملا صفر است. بنابراین در پیکربندی آزمون‌شده، آنچه اثر دارد \emph{انتخاب} ناحیه‌ی محافظت‌شده است و نه شدت مهار آن با جمله‌ی $\ell_2$. این یافته با نسخه‌ی پنج‌بذری همین کنترل‌ها تفاوت دارد، جایی که بازه‌ی \lr{CIFAR-100} صفر را در بر نمی‌گرفت؛ افزودن بذر آن جدایی را از میان برد.

\paragraph{چهار. فیلتر صریح اشتراک در \emph{این پیاده‌سازی} اضافی است.} بازوی رتبه‌بندی‌بدون‌اشتراک، بازوی کامل را \emph{دقیقا} بازتولید می‌کند؛ همه‌ی اختلاف‌های جفتی صفرند. اما تفسیر این یکسانی باید دقیق باشد و \textbf{ضرورت عمومی هم‌پوشانی را رد نمی‌کند}. دلیلش ساختاری است: در این بازو رتبه‌بندی روی کل زیرشبکه‌ی هدف $\Mf$ انجام می‌شود، ولی ماسک ترمیم \emph{مستقل از حالت کنترل} همان $\Mf\cap\Mr$ می‌ماند و گرادیان بیرون آن صفر می‌شود. پس هر مختصه‌ای که رتبه‌بندی بیرون $\Mr$ انتخاب کند، در عمل هرگز حرکت نمی‌کند و لنگر روی آن بی‌اثر است. به بیان دیگر، خود پیاده‌سازی فیلتر اشتراک را در پایین‌دست اعمال می‌کند؛ بنابراین این آزمایش نشان می‌دهد اعمال \emph{دوباره}ی آن در بالادست اضافی است، نه اینکه محدودکردن به ناحیه‌ی مشترک بی‌فایده باشد.

\paragraph{جمع‌بندی کنترل‌ها.} آنچه این کنترل‌ها به‌طور تجربی پشتیبانی می‌کنند، باریک‌تر از توصیف کامل سازوکار است: \emph{رتبه‌بندی مختصه‌های کاندید بر پایه‌ی حساسیت نگه‌داری، با یک بودجه‌ی ثابت}. دو جزء دیگر سهم مستقل قابل‌اندازه‌گیری نشان نمی‌دهند، اما این به‌معنای بی‌فایده‌بودن آن‌ها نیست: جریمه‌ی $\ell_2$ تنها شدت مهار را تنظیم می‌کند در حالی که پشتیبان‌ها مستقل از آن ساخته می‌شوند، و فیلتر اشتراک در پایین‌دست پیاده‌سازی به‌هرحال اعمال می‌شود. این کنترل‌ها برای اثبات \emph{ضرورت} همه‌ی اجزای طراحی کافی نیستند. این محدودیت در بخش \ref{sec:concl-limitations} به‌عنوان محدودیت ادعای سازوکاری ثبت می‌شود و در بخش \ref{sec:concl-future} به یک پیشنهاد ساده‌سازی تبدیل می‌شود.

\section{رژیم پیش‌آموزش‌دیده‌ی \lr{PEFT}}
\label{sec:res-pretrained}

جدول \ref{tab:res-pretrained} فرض روش‌های کارآمد پارامتری را برقرار می‌کند: \lr{LoRA} و \lr{PALL-Adapter} هر دو روی یک \lr{ResNet-18} منجمد و پیش‌آموزش‌دیده‌ی \lr{ImageNet} می‌نشینند. ورودی‌های \lr{CIFAR} از نرمال‌سازی مجموعه‌داده به آماره‌های \lr{ImageNet} که بدنه انتظار دارد تبدیل می‌شوند؛ \lr{TinyImageNet} از پیش همان آماره‌ها را دارد. این تصحیح نرمال‌سازی مهم است و در پیوست \ref{ch:appendix} توضیح داده شده است.

\input{tables/res_pretrained}

با بدنه‌ی پیش‌آموزش‌دیده، هر دو روش به‌طور چشمگیری بهبود می‌یابند: \lr{PALL-Adapter} به $0.9841$ روی \lr{CIFAR-10}، $0.8277$ روی \lr{CIFAR-100} و $0.9047$ روی \lr{TinyImageNet} می‌رسد و مقادیر متناظر \lr{LoRA} برابر $0.9848$، $0.8215$ و $0.8980$ است. اختلاف \lr{CIFAR-10} نسبت به پراکندگی بذرها ناچیز است، در حالی که آداپتر روی \lr{CIFAR-100} و \lr{TinyImageNet} به‌طور متوسط کمی بالاتر می‌ایستد. مقایسه‌ی این نقطه‌های پایان با هم، مقایسه‌ی رفتار انتهایی دو معماری است و تفاوت را به ماسک نرم نسبت نمی‌دهد؛ آن پرسش، موضوع بخش بعد است.

این جدول در کنار جدول‌های \ref{tab:res-main-c10} و \ref{tab:res-main-c100} همان نکته‌ی مهم را روشن می‌کند: عملکرد پایین دو سطر \lr{PEFT} در پروتکل از صفر، ناشی از سازوکار فراموشی آن‌ها نبود، بلکه ناشی از نشستن روی یک بازنمایی تصادفی بود.

\section{انتساب مؤلفه‌های \lr{PALL-Adapter}: یک نتیجه‌ی منفی}
\label{subsec:res-adapter-components}

این بخش به \lr{RQ5} پاسخ می‌دهد و پاسخ، منفی است. پنج بازوی هم‌تراز زیر خط لوله‌ی پیش‌آموزش‌دیده‌ی تصحیح‌شده و با دنباله‌های یکسان (سه بذر) مؤلفه‌های زمان درخواست را جدا می‌کنند: \emph{فقط بازنشانی} تنها آداپتر هدف و برش طبقه‌بند آن را بازنشانی می‌کند؛ \emph{بازنشانی+ترمیم} ترمیم نگه‌داری را اضافه می‌کند؛ \emph{یکنواخت} محافظت هم‌پوشانی را از به‌روزرسانی مشترک برمی‌دارد؛ \emph{ماسک/بدون‌صعود} گام صعود ذخیره‌شده‌ی طبقه‌بند را حذف می‌کند؛ و \emph{کامل} همه‌ی مؤلفه‌ها را دارد. جدول \ref{tab:res-adapter-components} نتیجه‌ی این پنج بازو را می‌دهد.

\input{tables/res_adapter_components}

سه یافته از این جدول بیرون می‌آید.

\paragraph{ماسک نرم در نقطه‌ی پایان بی‌اثر است.} بازوی کامل و بازوی یکنواخت حداکثر $0.0006$ در $\Afinal$ یا بیشترین افت با هم اختلاف دارند، و بازوی کامل و ماسک/بدون‌صعود در هر دو معیار \emph{یکسان}اند. چون بازوها از نظر مولد اعداد تصادفی هم‌تراز طراحی شده‌اند، این یکسانی نشانه‌ی نوفه نیست؛ نشانه‌ی یک مؤلفه‌ی واقعا بی‌اثر است.

\paragraph{صعود طبقه‌بند تنها مؤلفه‌ی با اثر قابل‌اندازه‌گیری است.} حذف آن، نگه‌داری را دست‌نخورده می‌گذارد اما وظیفه‌ی فراموش‌شده را به‌اندازه‌ی $0.0040$ روی \lr{CIFAR-100} و $0.0017$ روی \lr{CIFAR-10} از سطح تصادفی دور می‌کند. یعنی این مؤلفه در سرکوب سهم دارد، هرچند کوچک، و به دقت نگه‌داری آسیب نمی‌زند.

\paragraph{روی \lr{CIFAR-100} فقط‌بازنشانی در واقع \emph{بهتر} است.} نگه‌داری $0.8324$ در برابر $0.8244$، بیشترین افت $0$ در برابر $0.0147$، و فاصله‌ی میانگین کوچک‌تری تا سطح تصادفی. بازه‌های بوت‌استرپ جفتی روی سه دنباله برای اختلاف دقت و اختلاف فاصله تا سطح تصادفی صفر را در بر می‌گیرند، در حالی که اختلاف بیشترین افت به سود فقط‌بازنشانی است.

نتیجه‌گیری صریح: سرکوب مشاهده‌شده در مسیر \lr{PALL-Adapter} عمدتا به \emph{بازنشانی مسیر هدف} نسبت داده می‌شود. به‌روزرسانی مشترکِ آگاه از هم‌پوشانی در این مسیر، مؤلفه‌ای است که از نظر نظری دامنه‌ی مشخصی دارد (بخش \ref{sec:prop-theory}) اما در نقطه‌ی پایان اندازه‌گیری‌شده پشتوانه‌ی تجربی ندارد. به همین دلیل، این پایان‌نامه مسیر آداپتر را یک گزینه‌ی اکتشافی ممیزی‌شده می‌داند و آن را به‌عنوان مشارکت دوم ارائه نمی‌کند.

\subsection{ماسک نرم پیوسته: جاروب شدت}
\label{subsec:res-conflict-gamma}

نتیجه‌ی منفی بخش پیش، ماسک نرم \emph{گسسته} را ارزیابی می‌کند: پارتیشن دودویی کامل/نرم/منجمد با یک ضریب ثابت. یک تبیین محتمل برای آن نتیجه این است که مشکل نه در ایده‌ی محافظت، بلکه در \emph{درشت‌دانگی} پارتیشن باشد. برای آزمودن این تبیین، نسخه‌ی پیوسته‌ای پیاده‌سازی و جاروب شد که در آن هر مختصه‌ی زیرمجموعه‌ی فراموشی ضریب خاص خود را می‌گیرد:
\begin{equation}
\label{eq:continuous-mask}
	m_i=\mathrm{clamp}\big(1-\gamma\,\hat c_i,\;0,\;1\big),
	\qquad
	\hat c_i=\frac{\mathrm{relu}\big(-\,g^{f}_i\,g^{r}_i\big)}{\max_j \mathrm{relu}\big(-\,g^{f}_j\,g^{r}_j\big)},
\end{equation}
که در آن $g^{f}$ و $g^{r}$ گرادیان فراموشی و نگه‌داری روی آداپتر مشترک‌اند و $\hat c_i$ انرژی تعارض بهنجارشده است. مقدار $\gamma=0$ به‌روزرسانی کامل را بازمی‌گرداند، پس بازوی کنترل درون خود جاروب قرار دارد و این آزمایش به هیچ جدول دیگری تکیه نمی‌کند.

\input{tables/res_conflict_gamma}

جدول \ref{tab:res-conflict-gamma} هم وضعیت پایانی و هم آمار خودِ ماسک را کنار هم می‌دهد؛ این دو باید با هم خوانده شوند، چون معنای یک نتیجه‌ی پوچ بسته به آنکه ماسک تغییر کرده باشد یا نه کاملا متفاوت است.

\paragraph{وضعیت پایانی حرکت نمی‌کند.} در سراسر جاروب، دامنه‌ی تغییر دقت نهایی روی \lr{CIFAR-10} صفر و روی \lr{CIFAR-100} تنها $0.0006$ است؛ بیشترین افت و دقت هدف نیز در همین حد جابه‌جا می‌شوند. با سه بذر، هیچ‌کدام از این دامنه‌ها از نوفه‌ی بذر قابل‌تفکیک نیستند.

\paragraph{اما ماسک واقعا تغییر می‌کند.} این نکته، تفاوت میان یک نتیجه‌ی منفی معنادار و یک اشکال پیاده‌سازی است. در $\gamma=0$ هر $4916$ مختصه‌ی زیرمجموعه‌ی فراموشی به‌روزرسانی کامل می‌گیرند، و از $\gamma=0.25$ به بعد حدود یک‌سوم آن‌ها به دسته‌ی «نرم» منتقل می‌شوند. سیگنال تعارض هم وجود دارد و ساختگی نیست: میان $40$ تا $45$ درصد مختصه‌ها انرژی تعارض ناصفر دارند. پس نه ماسک ثابت مانده و نه سیگنال غایب است.

\paragraph{تبیین: دنباله‌ی بسیار نازک.} توضیح در توزیع $\hat c$ نهفته است. این کمیت با بیشینه بهنجار می‌شود، پس $\max_i\hat c_i=1$ است؛ اما \emph{میانگین} آن تنها $0.00106$ روی \lr{CIFAR-10} و حدود $0.004$ روی \lr{CIFAR-100} است، یعنی سه مرتبه‌ی بزرگی کمتر از بیشینه. انرژی تعارض بنابراین در دنباله‌ای بسیار نازک متمرکز است و اکثریت قاطع مختصه‌ها $\hat c_i\approx0$ دارند. با ضریب \emph{خطی} رابطه‌ی \eqref{eq:continuous-mask}، چنین مختصه‌ای حتی در بزرگ‌ترین $\gamma$ آزموده‌شده ضریبی نزدیک به یک می‌گیرد. سهم مختصه‌هایی که ضریبشان واقعا زیر یک‌دوم می‌افتد، در $\gamma=2$ هنوز کمتر از یک درصد است. برچسب «نرم» در جدول بنابراین گمراه‌کننده است: آن مختصه‌ها اسما نرم‌اند اما عددا تقریبا کامل به‌روز می‌شوند.

\paragraph{نتیجه.} تبیین درشت‌دانگی رد می‌شود. ریزکردن پارتیشن به یک ضریب پیوسته‌ی هر-مختصه، رفتار انتها‌به‌انتها را تغییر نمی‌دهد، و دلیلش نیز مشخص است: با این صورت‌بندی، $\gamma$ باید دو تا سه مرتبه‌ی بزرگی بزرگ‌تر باشد تا مختصه‌ی نوعی را به‌طور محسوس تضعیف کند. این یافته نتیجه‌ی منفی بخش پیش را تقویت می‌کند و هم‌زمان یک مسیر مشخص برای کارهای آینده باز می‌گذارد: ماسکی که به‌جای \emph{مقدار} $\hat c$ بر \emph{رتبه}‌ی آن بنا شود، از این مشکل مقیاس مصون است. ضمنا این جاروب همان سنجه‌ی جهت‌داری را که در بخش \ref{subsec:res-gradnorm} غایب بود فراهم می‌کند: نسبت هنجار گرادیان تنها بزرگی را می‌سنجید، در حالی که $\hat c$ از ضرب داخلی مختصه‌ای ساخته شده و جهت را در بر دارد.

\section{نسخه‌های \lr{PALL} زیر پروتکل هم‌پوشانی‌سنگین}
\label{sec:res-overlap-heavy}

این پروتکل از صفر و با سه ایپاک به‌ازای هر وظیفه، تنها سه نسخه‌ی \lr{PALL} را مقایسه می‌کند و رژیم یا دنباله‌ی پروتکل استاندارد را به اشتراک نمی‌گذارد؛ بنابراین اعداد آن با جدول‌های \ref{tab:res-main-c10} و \ref{tab:res-main-c100} مخلوط نمی‌شوند. جدول \ref{tab:res-overlap-heavy} این مقایسه را نشان می‌دهد.

\input{tables/res_overlap_heavy}

روی هر دو مجموعه‌ی \lr{CIFAR}، \lr{EPALL} بالاترین دقت نگه‌داری میان سه نسخه را دارد ($0.9152$ در برابر $0.9087$ و $0.5093$ در برابر $0.5013$) و هم‌زمان بیشترین افت را از $0.0040$ به $0.0008$ و از $0.0187$ به $0.0047$ کاهش می‌دهد. \lr{PALL-Adapter} در جهت مخالف معاوضه می‌کند: دامنه‌ی ترمیم کوچک‌تر و $A_u$ نزدیک به سطح تصادفی، به بهای دقت نگه‌داری وقتی آموزش از صفر آغاز می‌شود.

روی \lr{TinyImageNet} — که سخت‌ترین محک این پایان‌نامه با $20$ وظیفه است — \lr{EPALL} برتری دقت بزرگ‌تری نشان می‌دهد ($0.4533$ در برابر $0.3365$). با این حال بیشترین افت هر دو نسخه‌ی کامل بالا می‌ماند ($0.0840$ و $0.0853$)، که نشان می‌دهد در تنظیم بیست‌وظیفه‌ای و سه‌ایپاک، ظرفیت شبکه برای همه‌ی وظایف کافی نیست و مسئله دیگر فقط مسئله‌ی هم‌پوشانی نیست. بازه‌های میان‌بذری این سطرها هم‌پوشانی دارند، پس نتایج سه‌بذری این جدول روند توصیفی‌اند و رتبه‌بندی تاییدشده نیستند.

\section{هزینه: دامنه‌ی ترمیم و حالت مقیم}
\label{sec:res-storage}

\subsection{نسبت اسمی دامنه‌ی ترمیم}
\label{subsec:res-rupd}

\lr{PALL-Adapter} دامنه‌ی اسمی ترمیم نگه‌داری را نسبت به نسخه‌های تمام‌شبکه به‌شدت کاهش می‌دهد: از $0.0240$ به $0.0030$ روی \lr{CIFAR-10} (کاهش $87.5$ درصدی)، از $0.0210$ به $0.0055$ روی \lr{CIFAR-100} (حدود $74$ درصد) و از $0.0960$ به $0.0292$ روی \lr{TinyImageNet} (حدود $70$ درصد).

باید تصریح شود این نسبت‌ها چه چیزی را نمی‌سنجند. آن‌ها تنها دامنه‌ی پارامترهای آموزش‌پذیر در ماسک ترمیم را می‌پوشانند و بنابراین محاسبات انتها‌به‌انتها، حافظه‌ی اوج، زمان دیواری یا تعداد کل مختصه‌های تغییریافته را اثبات نمی‌کنند؛ به‌علاوه، ذخیره‌سازی آداپترهای مخصوص وظیفه با تعداد وظایف رشد می‌کند. به همین دلیل این نسبت‌ها در این پایان‌نامه تشخیص پیاده‌سازی نامیده می‌شوند و نه معیار کارایی. شکل \ref{fig:res-tradeoff} این نسبت را در برابر دو معیار کیفیت نشان می‌دهد.

\begin{figure}[htbp]
	\centering
	\begin{subfigure}{0.49\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{tradeoff_updated_vs_final_accuracy_by_dataset_regime.pdf}
		\caption{در برابر دقت نهایی}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.49\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{tradeoff_updated_vs_worstdrop_by_dataset_regime.pdf}
		\caption{در برابر بیشترین افت}
	\end{subfigure}
	\caption[مصالحه‌ی دامنه‌ی ترمیم و کیفیت]{مصالحه‌ی میان نسبت اسمی دامنه‌ی ترمیم و دو معیار کیفیت، به تفکیک مجموعه‌داده و رژیم. محور افقی یک تشخیص پیاده‌سازی است و نه سنجه‌ی کارایی انتها‌به‌انتها.}
	\label{fig:res-tradeoff}
\end{figure}

\subsection{حسابداری حالت مقیم}
\label{subsec:res-storage-acct}

چون نسبت دامنه‌ی ترمیم یک اندازه‌گیری حافظه نیست، جدول \ref{tab:res-storage} حالت تنسوری مقیم را روی دنباله‌های هم‌تراز بذر صفر به‌طور مستقیم حسابداری می‌کند. \lr{EPALL} همه‌ی وظایف را در یک مدل مشترک با ماسک‌های پراکنده و پشتیبان‌ها نگه می‌دارد، در حالی که \lr{CLPU} یک شبکه‌ی کامل به‌ازای هر وظیفه‌ی فعال دارد.

\input{tables/res_storage}

\lr{CLPU} در نتیجه میان $1.6$ تا $2.2$ برابر حالت حسابداری‌شده مصرف می‌کند و رشد آن به‌ازای هر وظیفه‌ی فعال $3.4$ تا $3.8$ برابر سریع‌تر است. این عدد، معنای واقعی «سقف بالا» بودن \lr{CLPU} را روشن می‌کند: برتری کوچک آن در دقت، با هزینه‌ی حالت مقیمی خریده می‌شود که با تعداد وظایف خطی رشد می‌کند. شکل \ref{fig:res-storage} همین ترکیب را نمایش می‌دهد.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.78\columnwidth]{aaai_storage.pdf}
	\caption[ترکیب حالت مقیم]{ترکیب حالت تنسوری مقیم در بیشترین تعداد وظیفه‌ی فعال. سهم \lr{EPALL} میان مدل پایه، ماسک‌ها و پشتیبان‌های پراکنده تقسیم می‌شود، در حالی که سهم غالب \lr{CLPU} از شبکه‌های جانبی هر-وظیفه می‌آید.}
	\label{fig:res-storage}
\end{figure}

\section{ممیزی حریم خصوصی و ماندگاری}
\label{sec:res-privacy}

\subsection{حمله‌ی استنتاج عضویت اصلاح‌شده}
\label{subsec:res-mia}

جدول \ref{tab:res-mia} نتیجه‌ی \lr{MIA} افزایش‌داده‌ی هم‌تراز را نشان می‌دهد؛ در این نسخه نمونه‌های عضو و غیرعضو با پیش‌پردازش یکسان و بدون افزایش‌داده سنجیده می‌شوند و امتیازهای مساوی با رتبه‌ی میانگین تفکیک می‌شوند.

\input{tables/res_mia}

هر دو مقدار پیش و پس از درخواست به $0.5$ نزدیک‌اند. تفسیر درست این نتیجه محتاطانه است: حمله‌ی آستانه‌ی زیان در این تنظیم توان تفکیک کافی ندارد، پس نتیجه‌ی تهی به‌دست‌آمده \emph{شاهد حذف نیست} و تنها می‌گوید این آزمون خاص چیزی پیدا نکرد. برای ادعای قوی‌تر، حمله‌های مبتنی بر مدل سایه \cite{carlini2022membership} یا ممیزی صوری \cite{steinke2023privacy} لازم است. شکل \ref{fig:res-mia} همین مقادیر را در سه رژیم نشان می‌دهد.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\columnwidth]{mia_before_after_by_dataset_regime.pdf}
	\caption[\lr{AUC} حمله‌ی عضویت پیش و پس از درخواست]{\lr{AUC} حمله‌ی استنتاج عضویت پیش و پس از درخواست فراموشی، به تفکیک مجموعه‌داده و رژیم. خط افقی سطح $0.5$ را نشان می‌دهد. نزدیکی به این خط، نتیجه‌ی تهیِ یک آزمون کم‌توان است.}
	\label{fig:res-mia}
\end{figure}

\subsection{آماره‌ی تفکیک‌پذیری حمله‌ی آستانه}
\label{subsec:res-epsilon}

نتیجه‌ی تهیِ بخش پیش تنها می‌گوید یک آزمون کم‌توان چیزی پیدا نکرد. سنجه‌ی حساس‌تر آن است که به‌جای \lr{AUC} میانگین، بدترین نقطه‌ی کاری همان حمله را ببینیم. برای این کار عضو را کلاس مثبت می‌گیریم و روی شبکه‌ای از آستانه‌ها، کران پایین یک‌طرفه‌ی ۹۵ درصد کلاپر-پیرسون برای نرخ مثبت درست و کران بالای همان‌گونه برای نرخ مثبت نادرست را می‌سازیم؛ آنگاه
\begin{equation}
	\label{eq:eps-hat}
	S=\max_{\text{آستانه}}\ \log\frac{\mathrm{TPR}_{\text{کران پایین}}}{\mathrm{FPR}_{\text{کران بالا}}},
\end{equation}
که در صفر بریده می‌شود. \textbf{این کمیت را نباید پارامتر حریم خصوصی تفاضلی یا کران پایین آن خواند.} بازخورد بررسی، انجام یک حسابرسی تجربی حریم خصوصی و گزارش $(\epsilon,\delta)$ را خواسته بود. آزمایش انجام شد و نتیجه‌ی آن در همین جدول است، اما خوانش $(\epsilon,\delta)$ آن پذیرفته نشد؛ دلیلش این است که هرچند شکل ظاهری رابطه به ممیزی‌های صوری شبیه است، سه شرط لازم برقرار نیست: آستانه روی همان داده‌ای انتخاب می‌شود که آماره روی آن ارزیابی می‌شود، ساختار مجموعه‌داده‌های همسایه وجود ندارد، و شمول تصادفی و پارامتر $\delta$ نیز تعریف نشده‌اند. بنابراین $S$ در این پایان‌نامه صرفا یک \emph{آماره‌ی تفکیک‌پذیری} برای همین حمله‌ی خاص است. یک ممیزی صوری واقعی در سبک \cite{steinke2023privacy} انجام نشده و در بخش \ref{sec:concl-future} به‌عنوان کار آینده ثبت شده است. جدول \ref{tab:res-epsilon} نتیجه را می‌دهد.

\input{tables/res_epsilon}

این جدول یک یافته‌ی صریحا \emph{نامساعد} برای روش پیشنهادی دارد و همان‌گونه گزارش می‌شود. پیش از درخواست، $S$ برای \lr{EPALL} روی هر دو مجموعه‌داده دقیقا صفر است؛ پس از درخواست، میانگین آن به $0.0993$ و $0.0995$ و بیشینه‌ی آن به $0.4555$ و $0.5544$ می‌رسد. یعنی همان عملیاتی که قرار بود اثر وظیفه‌ی هدف را بزداید، در این ممیزی نمونه‌های آن وظیفه را \emph{تفکیک‌پذیرتر} کرده است. تفسیر محتاطانه‌ی این مشاهده چنین است: بازنشانی و ترمیم، رفتار مدل روی نمونه‌های وظیفه‌ی حذف‌شده را از الگوی نمونه‌های دیده‌نشده دور می‌کند و همین اختلاف، سیگنالی برای حمله می‌سازد. دقت هدف نزدیک به سطح تصادفی (جدول‌های \ref{tab:res-main-c10} و \ref{tab:res-main-c100}) با این یافته در تضاد نیست: مدل \emph{درست} پاسخ نمی‌دهد، اما \emph{به‌گونه‌ی متفاوتی} اشتباه می‌کند.

سه نکته‌ی مقایسه‌ای هم مهم است. \lr{CLPU} در هر دو مجموعه‌داده پس از درخواست به $S=0$ می‌رسد، که پیامد ساختاری حذف کامل مدخل است و نشان می‌دهد این آماره در حالت مطلوب واقعا صفر برمی‌گرداند. مسیر \lr{PALL-Adapter} و \lr{LoRA} روی بدنه‌ی منجمد مقادیر بسیار کوچک‌تری دارند، که با کوچک‌تربودن دامنه‌ی به‌روزرسانی آن‌ها سازگار است. و سرانجام تأکید می‌شود که $S$ تنها در یک جهت اطلاع‌بخش است: مقدار بزرگ نشان‌دهنده‌ی تفکیک‌پذیری واقعی است، اما مقدار کوچک هیچ تضمینی نمی‌دهد، چون ممکن است صرفا حمله‌ی به‌کاررفته ضعیف باشد. بنابراین این جدول شاهدی علیه خوانش «حذف انجام شد» فراهم می‌کند و شاهدی به سود آن نمی‌سازد.

\subsection{نسبت هنجار گرادیان}
\label{subsec:res-gradnorm}

معیار آخر این بخش، شدت \emph{تداخل} را در لحظه‌ی درخواست می‌سنجد و نه کیفیت فراموشی را: نسبت هنجار $\ell_2$ گرادیان وظیفه‌ی هدف به گرادیان وظایف نگه‌داری‌شده، هر دو روی زیرشبکه‌ی مشترک و پیش از بازنشانی. جدول \ref{tab:res-gradnorm} این نسبت را می‌دهد.

\input{tables/res_gradnorm}

مقادیر در بازه‌ی $0.67$ تا $0.80$ قرار می‌گیرند، یعنی دو سیگنال \emph{هم‌مرتبه} هستند: گرادیان فراموشی نه آن‌قدر بزرگ است که گرادیان نگه‌داری را بی‌اثر کند و نه آن‌قدر کوچک که نادیده گرفته شود.

\textbf{اما این معیار تعارض را نمی‌سنجد.} چون $\|g_f\|_2/\|g_r\|_2$ تنها نسبت \emph{بزرگی} است و هیچ اطلاعاتی از جهت ندارد، دو گرادیان می‌توانند این نسبت را برابر یک داشته باشند و در عین حال کاملا هم‌راستا یا کاملا متضاد باشند. بنابراین این جدول تنها نشان می‌دهد دو سیگنال در ناحیه‌ی مشترک \emph{قابل مقایسه‌اند}، و ادعای «رقابت» یا «تعارض» از آن بیرون نمی‌آید. سنجش تعارض به معیارهای جهت‌دار نیاز دارد — کسینوس زاویه، ضرب داخلی، یا درصد مختصه‌هایی که علامت مخالف دارند — که هیچ‌کدام در این کار محاسبه نشده‌اند و در بخش \ref{sec:concl-future} به‌عنوان کار آینده ثبت می‌شوند. تفاوت میان \lr{EPALL} و \lr{PALL} اصلی در این معیار کوچک و در حد انحراف معیار است، که انتظار هم همین بود: این نسبت پیش از اعمال محافظت اندازه‌گیری می‌شود و ویژگی \emph{مسئله} است و نه ویژگی روش.

\subsection{مرجع بازآموزی هم‌روش}
\label{subsec:res-retrain}

آزمون سخت‌گیرانه‌تر، مقایسه با یک نمونه‌ی تازه از همان روش و همان معماری است که از ابتدا بدون وظیفه‌ی هدف آموزش دیده باشد. جدول \ref{tab:res-retrain} نتیجه را برای دو بذر می‌دهد.

\input{tables/res_retrain}

توافق روی وظایف نگه‌داری‌شده در بازه‌ی $0.60$ تا $0.96$ قرار می‌گیرد که نشان می‌دهد مدل فراموش‌کرده و مرجع بازآموزی، رفتار نسبتا مشابهی روی وظایف فعال دارند. اما توافق روی وظیفه‌ی فراموش‌شده میان $0.00$ و $0.98$ نوسان می‌کند. این ممیزی دو‌بذری \emph{شاهدی برای هم‌ارزی با بازآموزی دقیق فراهم نمی‌کند}: مدل فراموش‌کرده و مرجع ممکن است هر دو در سطح تصادفی کار کنند اما به‌طور یکسان اشتباه نکنند. با این حال، بدون کالیبراسیون «بازآموزی در برابر بازآموزی» نمی‌توان صرفا از این نوسان، حذف دقیق توزیعی را به‌طور صوری رد کرد.

\subsection{ماندگاری سرکوب}
\label{subsec:res-persistence}

درس روش‌شناختی کارهایی مانند \lr{UnCLe} \cite{adhikari2026uncle} و \lr{CATA} \cite{lin2026cata} این بود که دقت بی‌درنگ پس از درخواست کافی نیست. جدول \ref{tab:res-persistence} وظیفه‌ی فراموش‌شده را از لحظه‌ی حذف تا پایان دنباله‌ی درخواست‌ها دنبال می‌کند. سنجه‌ی این ممیزی \emph{فاصله تا شانس} $|A_u-c|$ است نه دقت خام: هدف حذف، رساندن وظیفه به سطح تصادفی است، پس دور شدن از شانس در هر دو جهت شکست است و افزایش دقت خام به‌تنهایی معیار درستی نیست.

نکته‌ی دوم آن است که در \emph{دو بذر این ممیزی} ($0$ و $1$)، دنباله‌ی درخواست‌ها پس از نخستین حذف \emph{درخواست آموزشی هم دارد}؛ در بذر $0$ الگوی \lr{CIFAR-10} به‌صورت \lr{T T T T F T F F} و الگوی \lr{CIFAR-100} به‌صورت \lr{T$\times$7 F T T F T F} است. این الگو میان بذرها \emph{یکسان نیست} و نباید به کل مجموعه تعمیم داده شود: موقعیت حذف‌ها با بذر تغییر می‌کند و در بذر $5$ مجموعه‌داده‌ی \lr{CIFAR-100} هر سه حذف در انتهای دنباله می‌آیند (\lr{T$\times$10 F F F})، یعنی هیچ درخواست آموزشی پس از نخستین حذف وجود ندارد. بنابراین می‌توان رانش وظیفه‌ی فراموش‌شده را به دو سهم جمع‌پذیر تفکیک کرد: سهمی که از درخواست‌های آموزشی میان دو حذف می‌آید و سهمی که از خود درخواست‌های حذف بعدی می‌آید. این تفکیک از روی شناسه‌ی درخواست هر رویداد و مقادیر پیش و پس از هر حذف محاسبه می‌شود و فاصله‌ای که هیچ درخواست آموزشی در آن نباشد، به‌طور ساختاری سهم صفر می‌گیرد.

\input{tables/res_persistence}

بدترین بازگشت مشاهده‌شده روی \lr{CIFAR-10} برابر $0.0490$ برای \lr{PALL} و $0.0370$ برای \lr{EPALL} و $0.0060$ برای \lr{PALL-Adapter} است؛ روی \lr{CIFAR-100} برابر $0.0500$ برای \lr{PALL-Adapter} و $0.0100$ برای \lr{PALL} و \lr{EPALL}. \lr{LoRA} و \lr{CLPU} بازگشت دقیقا صفر دارند، چون پارامترهای وظیفه‌ی حذف‌شده در آن‌ها از پارامترهای وظایف بعدی جدا هستند و هیچ درخواست بعدی آن‌ها را لمس نمی‌کند.

تفکیک سهم‌ها نتیجه‌ی غیرمنتظره‌ای می‌دهد. روی \lr{CIFAR-100}، سهم درخواست‌های آموزشی برای هر دو نسخه‌ی \lr{PALL} \emph{منفی} است ($-0.0090$ و $-0.0120$)؛ یعنی یادگیری وظایف تازه، وظیفه‌ی فراموش‌شده را به شانس نزدیک‌تر می‌کند نه دورتر. روی \lr{CIFAR-10} همین سهم اندکی مثبت است ($+0.0019$ و $+0.0063$) اما بخش عمده‌ی رانش \lr{PALL} از درخواست‌های \emph{حذف} بعدی می‌آید ($+0.0111$)، در حالی که همین سهم برای \lr{EPALL} منفی است ($-0.0053$). خوانش این تفاوت آن است که حفاظت رتبه‌بندی‌شده، علاوه بر مهار آسیب به وظایف نگه‌داری‌شده، مانع از آن می‌شود که حذف‌های بعدی وظیفه‌ی پیش‌تر حذف‌شده را دوباره از سطح تصادفی دور کنند. با این حال هیچ‌کدام از این تفاوت‌ها با دو بذر و چهار وظیفه‌ی دارای افق پیگیری، آزمون معناداری را برنمی‌تابد؛ آن‌ها را باید مشاهده‌ی توصیفی خواند. جمع‌بندی این است که سرکوب ماندگار است اما ناوردا نیست، و منبع غالب ناوردایی درخواست‌های حذف بعدی است نه یادگیری تازه. شکل \ref{fig:res-persistence} مسیرها را نشان می‌دهد.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\columnwidth]{forgetting_persistence.pdf}
	\caption[مسیر فاصله تا شانس وظیفه‌ی فراموش‌شده پس از حذف]{مسیر فاصله تا شانس $|A_u-c|$ برای وظیفه‌ی فراموش‌شده، از لحظه‌ی حذف به بعد. منحصرا نخستین وظیفه‌ی حذف‌شده‌ی هر اجرا دنبال می‌شود. برچسب هر نقطه‌ی محور افقی نشان می‌دهد که تغییر تا آن نقطه از درخواست‌های آموزشی میان‌راهی آمده است یا از یک درخواست حذف بعدی. چون موقعیت درخواست‌های حذف با بذر تغییر می‌کند، طول و ترکیب افق پیگیری میان بذرها یکسان نیست؛ منحنی‌ها تا آخرین نقطه‌ای رسم شده‌اند که همه‌ی اجراهای آن مجموعه‌داده در آن هم حاضرند و هم نوع رویداد یکسانی دارند، و پس از آن قطع می‌شوند تا میانگین‌گیری روی رویدادهای ناهم‌جنس رخ ندهد. به همین دلیل \lr{CIFAR-10} سه نقطه و \lr{CIFAR-100} پنج نقطه دارد. هر نقطه‌ی هر منحنی میانگین \emph{دو بذر} همان روش است. جدول \ref{tab:res-persistence} از این محدودیت مصون است، چون آماره‌های آن به‌ازای هر اجرا و مستقل از شکل دنباله محاسبه می‌شوند.}
	\label{fig:res-persistence}
\end{figure}

\section{مطالعات حساسیت}
\label{sec:res-sensitivity}

\subsection{انتخاب لنگر}
\label{subsec:res-anchor}

بخش \ref{subsec:prop-repair} اشاره کرد که لنگرزدن به $\theta^{\mathrm{old}}$ یک ایراد مفهومی دارد: آن وزن‌ها خودشان اطلاعاتی از وظیفه‌ی فراموش‌شونده حمل می‌کنند. جدول \ref{tab:res-anchor} جایگزین $\theta^{\mathrm{reinit}}$ را روی هشت بذر هم‌تراز از پروتکل استاندارد می‌سنجد. چون \lr{\texttt{protect\_anchor}} فقط روی \lr{EPALL} اثر دارد، دو بازو تنها در همین یک تنظیم تفاوت دارند و بذرها جفت می‌شوند؛ بازوی \lr{\texttt{reinit}} زیر برچسب آزمایش مستقل \lr{\texttt{*\_standard\_reinit\_v1}} اجرا شده تا جدول‌های اصلی دست‌نخورده بمانند.

\input{tables/res_anchor}

نتیجه‌ی هشت‌بذری با مطالعه‌ی دوبذری پیشین هم‌خوان نیست و جهت آن روی دو مجموعه‌داده یکسان نیست. روی \lr{CIFAR-100} برتری با $\theta^{\mathrm{old}}$ است: فراموشی در هفت جفت از هشت جفت به سود آن است ($d_z = 1.06$)، بیشترین افت در شش جفت ($d_z = 1.04$، میانگین اختلاف $0.0089$) و دقت نهایی در شش جفت. روی \lr{CIFAR-10} تفاوت‌ها در حد نوسان بذرهاست و اندکی به سود $\theta^{\mathrm{reinit}}$ می‌چرخد، اما هیچ معیاری بیش از پنج جفت از هشت جفت را نمی‌گیرد و بزرگی اختلاف دقت $0.0013$ است. تنها معیاری که در هر دو مجموعه‌داده به سود $\theta^{\mathrm{reinit}}$ می‌ماند، فاصله‌ی دقت هدف تا شانس است و آن هم با اختلاف میانگین $0.0030$ روی \lr{CIFAR-100}. مهم‌تر از همه، این خانواده ده آزمون دارد و پس از اصلاح \lr{Holm} \emph{هیچ} معیاری در هیچ جهتی در سطح $0.05$ باقی نمی‌ماند؛ کوچک‌ترین $p$ خام برابر $0.035$ است که پس از اصلاح به $0.35$ می‌رسد.

پیش‌فرض پیاده‌سازی $\theta^{\mathrm{old}}$ است و همه‌ی جدول‌های این پایان‌نامه با همان تولید شده‌اند. سه دلیل پشت این انتخاب است. نخست، لنگر تنها روی $S_{\mathrm{crit}} \subseteq M_f \cap M_r$ عمل می‌کند و فراموشی از راه بازنشانی ناحیه‌ی انحصاری $M_f \setminus M_r$ رخ می‌دهد؛ پس لنگر یک اهرم \emph{نگه‌داری} است نه اهرم فراموشی، و باید عمدتا با معیارهای نگه‌داری داوری شود — جایی که روی \lr{CIFAR-100} برتری با $\theta^{\mathrm{old}}$ است و روی \lr{CIFAR-10} تفاوتی نیست. دوم، $\theta^{\mathrm{reinit}}$ آن استقلال از داده‌ای را که وعده می‌دهد در عمل نمی‌خرد: شاخه‌های بازنشانی ارثی همچنان به پشتیبان‌های پراکنده‌ی وابسته به داده‌ی قدیمی تکیه دارند، پس مسیر نشت از راه دیگری باز می‌ماند و محدودیت دامنه‌ی ادعای حذف (بخش \ref{sec:concl-limitations}) همچنان برقرار است. سوم، $\theta^{\mathrm{old}}$ در زمان درخواست نیز سریع‌تر است ($3.87$ ثانیه روی \lr{CIFAR-100} و $0.47$ ثانیه روی \lr{CIFAR-10} به‌طور میانگین). ایراد مفهومی $\theta^{\mathrm{old}}$ سر جای خود باقی است و در بخش \ref{sec:concl-limitations} به‌عنوان محدودیت ثبت می‌شود، اما شاهد هشت‌بذری آن را به توصیه‌ی عملی تبدیل نمی‌کند.

تصریح می‌شود که $\theta^{\mathrm{reinit}}$ در بازخورد بررسی‌ای که این کار دریافت کرد، به‌عنوان صورت‌بندی \emph{درست} جمله‌ی لنگر پیشنهاد شده بود؛ استدلال آن بازخورد این بود که توزیع مقدارگذاری اولیه از هر داده‌ای مستقل است، پس لنگر، اطلاعات وظیفه‌ی حذف‌شونده را دوباره وارد نمی‌کند. آن استدلال \emph{در سطح خودِ جمله‌ی لنگر} درست است و اینجا رد نمی‌شود. آنچه گزارش می‌شود دو چیز دیگر است. نخست، در سطح \emph{سامانه} کافی نیست: مسیر بازنشانی شاخه‌محور که از \lr{PALL} به ارث رسیده همچنان از پشتیبان‌های پراکنده‌ی وابسته به داده‌ی پیشین می‌خواند، پس مستقل‌کردن لنگر تنها یکی از دو مسیر وابستگی را می‌بندد و ادعای استقلال از داده در سطح کل درخواست برقرار نمی‌شود. دوم، همان صورت‌بندی، حفظ تضمین حذف دقیق را \emph{مشروط} بیان می‌کند: مشروط بر آنکه جمله‌ی حذف روی پارامترهای غیرمشترک به حذف دقیق برسد. ممیزی مرجع بازآموزی در بخش \ref{subsec:res-retrain} برای برقراری این شرط شاهدی به‌دست نمی‌دهد؛ توافق روی وظیفه‌ی هدف میان $0.00$ و $0.98$ نوسان می‌کند که با خوانش حذف دقیق سازگار نیست. بنابراین تغییر لنگر، مقدم آن گزاره‌ی شرطی را برقرار نمی‌کند و تالی آن نیز به‌دست نمی‌آید. در کنار این دو ملاحظه‌ی مفهومی، مقایسه‌ی هشت‌بذری بالا هیچ برتری تجربی‌ای برای $\theta^{\mathrm{reinit}}$ نشان نداد. به همین دلیل پیشنهاد بازخورد آزموده شد اما پذیرفته نشد، و دلیل نپذیرفتن آن در همین بخش مستند است.

\subsection{معیار اهمیت: بزرگی گرادیان در برابر انرژی تعارض}
\label{subsec:res-conflict}

جدول \ref{tab:res-conflict} دو معیار رتبه‌بندی معرفی‌شده در معادلات \eqref{eq:sensitivity} و \eqref{eq:conflict} را مقایسه می‌کند.

\input{tables/res_conflict}

نتیجه وابسته به مسیر و مجموعه‌داده است: انرژی تعارض به مسیر آداپتر روی \lr{CIFAR-10} کمک می‌کند (بیشترین افت از $0.0160$ به $0.0063$) اما به \lr{EPALL} روی \lr{CIFAR-100} آسیب می‌زند (بیشترین افت از $0.0060$ به $0.0230$). به همین دلیل بزرگی گرادیان معیار پیش‌فرض می‌ماند. نکته‌ی عملی مهم این است که معیار تعارض به گرادیان وظیفه‌ی هدف نیاز دارد و بنابراین باید در پنجره‌ی دسترسی پیش از حذف محاسبه شود؛ یعنی حتی اگر سود می‌داشت، هزینه‌ی معماری بیشتری داشت.

\subsection{انتقال میان‌معماری با \lr{ViT}}
\label{subsec:res-vit}

جدول \ref{tab:res-vit} همان مقایسه‌ی \lr{EPALL} و \lr{PALL} اصلی را روی یک بدنه‌ی \lr{ViT-T/8} \cite{dosovitskiy2021vit} تکرار می‌کند.

\input{tables/res_vit}

روی \lr{Split-CIFAR-100}، \lr{EPALL} بیشترین افت میانگین را از $0.0190$ به $0.0100$ کاهش می‌دهد و دقت را کمی بهبود می‌دهد ($0.3990$ در برابر $0.3921$)، در حالی که $A_u$ نزدیک به سطح تصادفی می‌ماند. روی \lr{Split-CIFAR-10} میانگین‌ها تقریبا برابرند. بنابراین انتقال به معماری ترنسفورمری وابسته به مجموعه‌داده است و با دو بذر هیچ ادعای معناداری مطرح نمی‌شود.

\subsection{عرض گلوگاه آداپتر}
\label{subsec:res-bottleneck}

جدول \ref{tab:res-bottleneck} جاروب عرض گلوگاه را نشان می‌دهد. نتیجه غیریکنواخت است: نه دقت و نه بیشترین افت با افزایش عرض روند مشخصی ندارند. بنابراین انتخاب $16$ یک پیش‌فرض \emph{فشرده} است و نه یک بهینه‌ی ادعاشده.

\input{tables/res_bottleneck}

شکل‌های \ref{fig:res-bottleneck} و \ref{fig:res-heatmap} نمای گرافیکی این جاروب و نقشه‌ی حرارتی دقت یک اجرای نماینده را نشان می‌دهند. علاوه بر این، جاروب ابرپارامتری پیش‌آموزش‌دیده‌ی مبتنی بر نرمال‌سازی قدیمی از شواهد تصحیح‌شده \emph{کنار گذاشته شده} است، چون پیش‌پردازش ورودی آن با بدنه‌ی \lr{ImageNet} هم‌خوان نبود.

\begin{figure}[htbp]
	\centering
	\begin{subfigure}{0.98\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{adapter_bottleneck_ablation.pdf}
		\caption{گلوگاه مخصوص وظیفه}
	\end{subfigure}
	\vspace{0.5em}

	\begin{subfigure}{0.98\columnwidth}
		\centering
		\includegraphics[width=\linewidth]{shared_bottleneck_ablation.pdf}
		\caption{گلوگاه مشترک}
	\end{subfigure}
	\caption[جاروب عرض گلوگاه آداپترها]{جاروب عرض گلوگاه مخصوص وظیفه و گلوگاه مشترک روی \lr{CIFAR-10}. هیچ‌یک از دو منحنی یکنوا نیست.}
	\label{fig:res-bottleneck}
\end{figure}

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.95\columnwidth]{representative_pall_adapter_accuracy_heatmap.pdf}
	\caption[نقشه‌ی حرارتی دقت یک اجرای نماینده]{نقشه‌ی حرارتی دقت وظیفه‌ها در طول یک اجرای نماینده‌ی \lr{PALL-Adapter}. سطرها وظیفه‌ها و ستون‌ها درخواست‌ها هستند؛ افت ستونی در نقطه‌ی هر درخواست فراموشی دیده می‌شود.}
	\label{fig:res-heatmap}
\end{figure}

\paragraph{ترکیب هم‌پوشانی.} لاگ‌های مسیر آداپتر نسبت $|\Scrit|/|\Sforget|$ را در بازه‌ی $0.35$ تا $0.56$ و کسر سفت‌محافظت‌شده را در بازه‌ی $0.07$ تا $0.11$ ثبت می‌کنند. این مقادیر نشان می‌دهند ناحیه‌ی بحرانی در آداپتر مشترک بخش قابل‌توجهی از ناحیه‌ی هدف است — یعنی شرایط برای اثرگذاری ماسک نرم فراهم بود — و بی‌اثربودن آن در نقطه‌ی پایان (بخش \ref{subsec:res-adapter-components}) به کوچک‌بودن ناحیه ربطی ندارد.

\section{تهدیدهای اعتبار}
\label{sec:res-threats}

\paragraph{اعتبار درونی.} خطر اصلی، انتساب بهبود به مؤلفه‌ی نادرست است. کاهش این خطر با سه سازوکار انجام شده است: جفت‌شدگی دنباله‌ها بر اساس بذر، کنترل‌های مؤلفه‌ای که هر بار فقط یک جزء را عوض می‌کنند، و هم‌ترازی مولد اعداد تصادفی در بازوهای آداپتر که باعث شد بی‌اثربودن ماسک نرم از نوفه قابل تفکیک باشد.

\paragraph{اعتبار آماری.} کف $p$ قابل‌دستیابی برای هر آزمون جداگانه است و به تعداد جفت‌های بی‌گره بستگی دارد؛ برای سطرهای بدون گره‌ی \lr{CIFAR-100} برابر $0.003906$ و برای دقت نهایی \lr{CIFAR-10} برابر $0.007812$ است. شش آزمون هم‌زمان با اصلاح \lr{Holm} تصحیح شده‌اند و چهار مورد از آن‌ها در سطح $0.05$ باقی می‌مانند. تحلیل‌های دو و سه بذری صریحا توصیفی برچسب خورده‌اند و در هیچ ادعای معناداری وارد نشده‌اند. بازه‌های بوت‌استرپ نیز توصیفی‌اند و آزمون فرض نیستند.

\paragraph{ناپایداری نتیجه با افزودن بذر.} یک هشدار روش‌شناختی مشخص از همین کار بیرون آمد: افزودن سه بذر به کنترل‌های مؤلفه‌ای، جدایی جمله‌ی لنگر روی \lr{CIFAR-100} را از میان برد (بخش \ref{subsec:res-components}). این نشان می‌دهد بازه‌های بوت‌استرپ نمونه‌ی کوچک تا چه اندازه می‌توانند شکننده باشند و چرا انتساب سازوکار باید با کم‌ترین تعداد بذری که نتیجه در آن پایدار است گزارش نشود.

\paragraph{اعتبار بیرونی.} همه‌ی نتایج روی سه محک تصویری با وظیفه‌های مصنوعی افرازشده به‌دست آمده‌اند. تعمیم به داده‌ی واقعی با مرزهای وظیفه‌ی نامشخص، به داده‌ی چندحالتی، یا به مقیاس‌های بزرگ‌تر آزموده نشده است. آزمون \lr{ViT} تنها یک نشانه‌ی ابتدایی از انتقال میان‌معماری می‌دهد.

\paragraph{اعتبار سازه‌ای.} خطر جدی‌تر، گرفتن معیارها به‌جای مفهوم است. سه نمونه‌ی مشخص: $A_u$ نزدیک به سطح تصادفی معیار حذف نیست، $\Rupd$ معیار کارایی نیست، و \lr{AUC} نزدیک به $0.5$ در \lr{MIA} معیار حریم خصوصی نیست. تمام متن این فصل بر همین تفکیک‌ها اصرار کرده است.

\paragraph{اعتبار محاسباتی.} زمان درخواست نرمال‌نشده است و روی یک پیکربندی سخت‌افزاری خاص اندازه‌گیری شده؛ بنابراین برای مقایسه‌ی مرتبه‌ای معتبر است و نه برای مقایسه‌ی دقیق.

\section{جمع‌بندی یافته‌ها}
\label{sec:res-summary}

پاسخ‌های تجربی به پرسش‌های پژوهش چنین است.

\begin{description}
	\item[\lr{RQ1} — بله، با دامنه‌ی مشخص.] \lr{EPALL} هم‌زمان $A_u$ را به سطح تصادفی می‌رساند و دقت نگه‌داری را نسبت به \lr{PALL} اصلی بهبود می‌دهد؛ این بهبود روی هر دو مجموعه‌داده پس از اصلاح \lr{Holm} باقی می‌ماند. بیشترین افت تنها روی \lr{CIFAR-100} معنادار می‌ماند.
	\item[\lr{RQ2} — عملا هم‌تراز در دقت، با هزینه‌ی زمان درخواست.] \lr{EPALL} روی \lr{CIFAR-10} کمتر از یک واحد درصد پایین‌تر از \lr{CLPU} است و روی \lr{CIFAR-100} در حد پراکندگی بذرها با آن برابر می‌ایستد، در حالی که \lr{CLPU} میان $1.6$ تا $2.2$ برابر حالت مقیم مصرف می‌کند. در مقابل، \lr{EPALL} در زمان درخواست از \lr{PALL} اصلی کندتر است.
	\item[\lr{RQ3} — شواهد مثبت اما مشروط؛ اثر علّی خالص اثبات نشده است.] محک موقعیت درخواست شاهدی توصیفی می‌دهد: بیشترین افت \lr{PALL} اصلی با رشد درجه بالا می‌رود و \lr{EPALL} مسطح می‌ماند، اما درجه‌ها هم‌پوشانی واقعی را کنترل نمی‌کنند. جاروب پراکندگی (بخش \ref{sec:res-overlap-direct}) با ثابت‌نگه‌داشتن هویت و موقعیت وظیفه‌ی هدف، هم‌پوشانی اندازه‌گیری‌شده را در بازه‌ای بیش از ده‌برابری جابه‌جا می‌کند و همبستگی رتبه‌ای مثبت و معناداری میان آن و سود حفاظت نشان می‌دهد؛ اما چون همان دستگیره ظرفیت را نیز تغییر می‌دهد، این شاهد در سطح اثر متقابل باقی می‌ماند و اثر علّی خالص هم‌پوشانی را جدا نمی‌کند.
	\item[\lr{RQ4} — تنها رتبه‌بندی با بودجه‌ی ثابت.] کیفیت انتخاب به‌روشنی و روی هر دو مجموعه‌داده مؤثر است. دو جزء دیگر سهم مستقل نشان نمی‌دهند، اما کنترل‌های موجود برای رد ضرورت آن‌ها کافی نیستند. افزون بر این، رتبه‌بندی همه‌ی معیارها را غالب نمی‌کند: محافظت بدون رتبه‌بندی بیشترین افت کمتری دارد.
	\item[\lr{RQ5} — نه.] ماسک نرم در نقطه‌ی پایان مسیر آداپتر اثر قابل‌اندازه‌گیری ندارد و سرکوب به بازنشانی مسیر هدف نسبت داده می‌شود.
\end{description}
