# Curated Results

- `stable/`: thesis-ready tables and summaries for the stable PALL experiments.
- `adapter/`: thesis-ready tables and summaries for the `pall_adapter` prototype.
- `aggregates/`: generated aggregate CSV/Markdown outputs built from `runs/`.

Raw run artifacts belong under `runs/` or `runs_archive/`, not here.
